# Feature Requests

本目录收录 `orchestrator` 的正式功能需求文档。Agent Process Console v1 已完成闭环；产品结构与发布边界分别由[信息架构](../design_doc/orchestrator/110-process-console-information-architecture.md)和[发布验收设计](../design_doc/orchestrator/116-process-console-release-acceptance.md)持续承载。Slack reaction 驱动的 Skill 任务自动化已发布（FR-107 至 FR-113 全部闭环删除），其设计、验证与用户指南现由 `docs/design_doc/orchestrator/118-`～`124-`、`docs/qa/orchestrator/155-`～`161-` 与 [用户指南](../guide/slack-reaction-skill-automation.md)承载；Managed Slack Connection、官方 App OAuth 与[每 workspace 独立 App provisioning](../design_doc/orchestrator/126-dedicated-slack-app-auto-provisioning.md)均已闭环。Agent 执行后端契约的供应商中立化已由 [Agent Driver 设计](../design_doc/orchestrator/127-agent-driver-abstraction.md)闭环；生产工作流的协调坍缩、冻结棘轮与退役标准由[协调 Strangler 收尾设计](../design_doc/orchestrator/136-coordination-strangler-completion.md)持续承载。FR-160 已闭环删除；QA harness 的共享守护进程拆解（`scripts/lib/gate_daemon.sh` 与 check 16）现由 [DD-174](../design_doc/orchestrator/174-qa-harness-daemon-teardown.md) 与 [QA 211](../qa/orchestrator/211-qa-harness-daemon-teardown.md) 承载。

## 当前条目


<!-- BEGIN GENERATED FR REGISTRY -->
> 由 `scripts/lib/fr_registry.rb` 从完整 `HEAD` 祖先历史生成：150 个历史编号 / 155 条历史路径，另有 13 条无 FR 文件历史的审阅例外；5 个编号存在多路径碰撞。浅克隆拒绝生成。

| ID | 标题 | 优先级 | 状态 | 来源 / 碰撞 |
|----|------|--------|------|-------------|
| FR-001 | Step 执行隔离与按需 Sandbox | P0 | Closed | git history |
| FR-002 | Daemon 控制面认证、鉴权与传输安全 | P0 | Closed | git history |
| FR-003 | Self-Referential Safety 约束语义收敛 | P1 | Closed | git history |
| FR-004 | DAG / 动态编排主路径化与可观测化 | P1 | Closed | git history |
| FR-005 | Daemon 生命周期治理与运行态指标补完 | P1 | Closed | git history |
| FR-006 | 彻底消除全局设定与实现纯粹的 Project-Only 架构 | — | Closed | collision (2): FR-006-project-scoped-isolation.md; FR-006-sandbox-network-allowlist-backend.md |
| FR-009 | 数据库迁移治理与持久化边界收敛 | — | Closed | git history |
| FR-010 | 控制面安全基线收紧与强制 mTLS | P0 | Closed | git history |
| FR-011 | validate/scheduler/runner 职责拆分与验证逻辑去重 | P1 | Closed | git history |
| FR-012 | SecretStore 密钥轮换、吊销与审计链 | P0 | Closed | git history |
| FR-013 | gRPC 控制面速率限制与 DoS 防护 | P0 | Closed | git history |
| FR-014 | 关键路径 `expect()` 清退与错误语义收敛 | P1 | Closed | git history |
| FR-015 | 高频 `clone()` 优化与共享所有权治理 | P2 | Closed | git history |
| FR-016 | 异步上下文锁模型收敛到 `tokio::sync::RwLock` | P1 | Closed | git history |
| FR-017 | Agent Drain 与 Enabled 开关 | P1 | Closed | git history |
| FR-018 | 用户指南编译验证对齐 | P1 | Closed | git history |
| FR-019 | 修复 libc 类型编译错误 | P0 | Closed | git history |
| FR-020 | 自动化 protoc 依赖安装 | P0 | Closed | git history |
| FR-021 | 审计并减少 expect() 调用 | P1 | Closed | git history |
| FR-022 | 补充公共 API 文档注释 | P1 | Closed | git history |
| FR-023 | 增加集成测试覆盖 | P2 | Closed | git history |
| FR-024 | 审计 unsafe 块 | P2 | Closed | git history |
| FR-026 | 事件表归档与 TTL 清理策略 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-027 | Worker 轮询优化 — Notify 唤醒机制 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-029 | Item-Scoped Git 工作目录隔离 | P0 | Closed | git history |
| FR-030 | Self-Evolution 数据库 Schema 对齐验证 | P1 | Closed | git history |
| FR-031 | generate_items 对 LLM 非标准 JSON 输出的容错解析 | — | Closed | git history |
| FR-032 | Daemon 进程崩溃韧性与 Worker 存活保障 | — | Closed | git history |
| FR-033 | Daemon 重启后孤立 Running Items 自动恢复 | — | Closed | git history |
| FR-034 | QA Testing 自引用安全防护 | — | Closed | git history |
| FR-035 | 退化循环检测与熔断机制 | — | Closed | git history |
| FR-036 | Plan Output 上下文溢出缓解 | — | Closed | git history |
| FR-037 | Dynamic Items 触发的循环溢出 — max_cycles 约束失效 | P1 | Closed | git history |
| FR-038 | Daemon 重启时在途步骤竞态 — task_completed 提前发出与动态 Item 状态丢失 | — | Closed | git history |
| FR-039 | Trigger 资源 — Cron 与事件驱动的任务自动创建 | — | Closed | git history |
| FR-040 | QA Agent 子进程绕过 Daemon PID Guard 杀死 Daemon | P1 | Closed | git history |
| FR-041 | Self-Restart 后 Socket 连接断裂导致后续步骤不可达 | P1 | Closed | git history |
| FR-042 | follow_task_logs 流式回调重构 — gRPC TaskFollow 端点从空流变为真实日志流 | P1 | Closed | git history |
| FR-043 | loop_guard 收敛条件表达式 | P1 | Closed | git history |
| FR-044 | Sandbox 写入拒绝检测与 writable_paths 完善 | P1 | Closed | git history |
| FR-045 | QA Agent 长生命周期命令防护 | P1 | Closed | git history |
| FR-046 | Agent 子进程 Daemon PID Guard 穿透防护 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-047 | Core Crate 拆分 Phase 1 — orchestrator-config 提取 | — | Closed | git history |
| FR-048 | Core Crate 拆分 Phase 2 — orchestrator-scheduler 提取 | — | Closed | git history |
| FR-049 | Prehook CEL 表达式接入 Pipeline Variables | P1 | Closed | git history |
| FR-050 | CLI UDS 连接回退鲁棒性 | P2 | Closed | git history |
| FR-051 | Workflow YAML 步骤定义未知字段警告 | — | Closed | git history |
| FR-052 | Inflight Wait Heartbeat-Aware Timeout | P1 | Closed | git history |
| FR-053 | Full-QA Workflow 大规模 Item 分发中断 — max_cycles_enforced 过早触发 | P0 | Closed | git history |
| FR-054 | Item 进度增量更新 — finalize_items 延迟导致 Progress 长时间为零 | P1 | Closed | git history |
| FR-055 | Parallel Spawn Stagger Delay | — | Closed | git history |
| FR-056 | Agent Health Policy 可配置化 | — | Closed | git history |
| FR-057 | orchestratord 真正 Daemon 化 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-058 | QA 自引用测试覆盖率恢复 — 场景级安全分级治理 | P1 | Closed | git history |
| FR-060 | 减少 QA 场景中的不安全操作 | — | Closed | git history |
| FR-061 | Daemon 日志环境变量覆盖 | P2 | Closed | git history |
| FR-062 | Agent Health 状态可观测性 | P2 | Closed | git history |
| FR-063 | GUI 架构设计 — Tauri + gRPC 安全客户端 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-064 | GUI 用户界面设计 — 许愿池 + 进度观察 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-065 | Agent 间通信接口草案 — Mailbox + Session Control Plane | — | Closed | git history |
| FR-066 | GUI 实时状态推送与许愿池数据隔离 | P0 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-067 | GUI CLI 功能对齐 — 补全缺失 RPC 覆盖 | — | Closed | git history |
| FR-068 | GUI 连接韧性与系统通知 | — | Closed | git history |
| FR-069 | GUI 体验打磨 — 主题切换 / 动画 / i18n / 响应式 / 构建分发 | — | Closed | git history |
| FR-070 | evo_apply_winner 可观测性增强 — 候选选择与代码应用决策日志 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-071 | 开源合规基础设施 | P0 | Closed | git history |
| FR-072 | 分发渠道扩展 — Docker 镜像与 Homebrew | P1 | Closed | git history |
| FR-073 | 文档站点与 Landing Page | P1 | Closed | git history |
| FR-074 | 可观测性导出 — Prometheus Metrics 端点 | P2 | Closed | git history |
| FR-075 | VS Code 扩展 — Manifest Schema Validation & Autocomplete | P2 | Closed | git history |
| FR-076 | GUI 正式发布 — Tauri App 打包分发 | P1 | Closed | git history |
| FR-077 | Linux Sandbox Filesystem Isolation Backend | P3 | Closed | collision (2): FR-077-linux-sandbox-filesystem-isolation.md; FR-077-workflow-template-library.md |
| FR-078 | Task Items 与 Event List CLI 命令 | P1 | Closed | git history |
| FR-079 | 数据生命周期治理 — 日志清理、DB 瘦身与自动化回收 | P1 | Closed | git history |
| FR-080 | Webhook Trigger 基础设施 — HTTP 事件入口与通用事件源扩展 | P0 | Closed | git history |
| FR-081 | Per-Trigger Webhook 认证与 CEL Payload 过滤 | P1 | Closed | git history |
| FR-082 | 集成 Manifest 包 — Slack / GitHub / Line 预制配置 | P2 | Closed | git history |
| FR-083 | CRD 插件系统 — Webhook 拦截器与自动化生命周期 | P3 | Closed | git history |
| FR-084 | Daemon Configuration Hot Reload | P2 | Closed | git history |
| FR-085 | Filesystem Trigger — 文件系统变更原生触发器 | P1 | Closed | collision (2): FR-085-filesystem-trigger.md; FR-085-long-running-agent-test-fixture.md |
| FR-086 | CLI Command to Simulate Agent Selection Logic | P3 | Closed | git history |
| FR-087 | Agent Health Policy CLI 测试夹具 — 自定义策略 QA 可验证性 | — | Closed | git history |
| FR-088 | QA Doctor CLI — 可观测性指标暴露 | — | Closed | git history |
| FR-089 | SecretStore 加密密钥紧急恢复机制 | — | Closed | git history |
| FR-090 | 轻量化单步执行 — orchestrator run 命令 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-091 | Linux Sandbox Filesystem Isolation Backend | P3 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-092 | Pipeline 变量 Spill 路径可配置 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-093 | 沙箱可配置读取路径白名单 | P2 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-094 | 自定义 Step ID 的显式 Scope 跨 Round-Trip 漂移修复 | P1 | Closed | legacy exception: Pre-registry README entry has no FR document path in the complete HEAD ancestry. |
| FR-095 | Process Timeline Read Model | P0 | Closed | git history |
| FR-096 | Attention Inbox | P0 | Closed | git history |
| FR-097 | Handoff Briefing and Safe Resume | P1 | Closed | git history |
| FR-098 | Agent Session Control Plane | P1 | Closed | git history |
| FR-099 | Source Events and Slack Process Binding | P1 | Closed | git history |
| FR-100 | Agent Process Console UI | P1 | Closed | git history |
| FR-101 | Canonical Control-Plane Action Audit Envelope | P0 | Closed | git history |
| FR-102 | Agent Session Control Plane Hardening And Acceptance | P1 | Closed | git history |
| FR-103 | Process Console Recovery, Attention Notifications, And Live E2E | P1 | Closed | git history |
| FR-104 | Process Console Operational Metrics And Local Dashboard | P1 | Closed | git history |
| FR-105 | Session RuntimePolicy Authority And Deterministic Control Gates | P0 | Closed | git history |
| FR-106 | Agent Process Console Release Acceptance And Rollback Runbook | P1 | Closed | git history |
| FR-107 | Slack Reaction Source Event Contract | P1 | Closed | git history |
| FR-108 | Source Task Template And Skill Invocation Resource | P1 | Closed | git history |
| FR-109 | Source Task Binding And Badge Matching Resource | P1 | Closed | git history |
| FR-110 | Slack Permalink Resolution And Canonical Task Routing | P0 | Closed | git history |
| FR-111 | Source Automation Reliability, Policy, And Operations | P1 | Closed | git history |
| FR-112 | Process Console Source Automation UI | P1 | Closed | git history |
| FR-113 | Slack Reaction Skill Automation Release Acceptance | P1 | Closed | git history |
| FR-114 | Managed Slack Connection 与官方 App OAuth 快速路径 | P0 | Closed | git history |
| FR-115 | 每 Workspace 独立 Slack App 自动 Provisioning | P0 | Closed | git history |
| FR-116 | A: Codex Driver 会话续接语义验证（Follow-up） | P3 | Closed | collision (2): FR-116-A-codex-session-resume-verification.md; FR-116-agent-driver-abstraction.md |
| FR-117 | A: 全局 Skill 目录属主/权限位校验（Follow-up） | P2 | Closed | collision (2): FR-117-A-global-skill-directory-ownership-check.md; FR-117-non-code-workspace-and-global-file-sharing.md |
| FR-118 | 协调塌缩 — 用 orchestrator-owned MCP 工具替换过渡态 CEL 层 | P1 | Closed | git history |
| FR-119 | Expert Resources 可达列表与受治理编辑闭环 | P1 | Closed | git history |
| FR-120 | Handoff 恢复审查对话框焦点生命周期 | P2 | Closed | git history |
| FR-121 | Attention Mutation 错误反馈与权威状态对账 | P1 | Closed | git history |
| FR-122 | CLI、Daemon 与 Tauri 边界层覆盖率治理 | P1 | Closed | git history |
| FR-123 | 受控 Slack Sandbox 持续认证与证据保鲜 | P1 | Closed | git history |
| FR-124 | 协调坍缩 Strangler 迁移收尾与遗留路径退役治理 | P1 | Closed | git history |
| FR-125 | 遗留协调机器分级退役（Deprecate → Remove） | P2 | Closed | git history |
| FR-126 | Agent 执行路径迁移 — Showcase 链接闭环与文档门禁补强 | P1 | Closed | git history |
| FR-127 | 治理门禁执行面补完 — QA 门禁 CI 接线与脚本执行面分类 | P0 | Closed | git history |
| FR-128 | 治理台账再生与审阅工具 — 消除手工 SHA256 维护摩擦 | P1 | Closed | git history |
| FR-129 | Skill 单一来源与镜像完整性 — 修复损坏的 `.agents` 镜像 | P1 | Closed | git history |
| FR-130 | Core Crate 拆分 Phase 3 — persistence 提取 | P1 | Closed | git history |
| FR-131 | 文档发布链路单一来源与链接完整性门禁 | P2 | Closed | git history |
| FR-132 | QA/DD 文档生命周期治理 — 退役标注与索引可导航性 | P2 | Closed | git history |
| FR-133 | 依赖策略门禁 — 重复版本、许可证与来源约束 | P3 | Closed | git history |
| FR-134 | 门禁执行事实校验 — 消除 FR-127 中"文本存在性即执行"的代理 | P1 | Closed | git history |
| FR-135 | 边界层覆盖率 job 恢复 — bash 3.2 空数组与产物路径 | P1 | Closed | git history |
| FR-136 | 持久化依赖收口决策 — 新 crate 是收口点还是又一个共享依赖 | P1 | Closed | git history |
| FR-137 | governance job 聚合清单的完整性断言 | P2 | Closed | git history |
| FR-138 | bash 3.2 兼容性扫描器的跨行词法状态与漏报面 | P2 | Closed | git history |
| FR-139 | 持久化收口门禁的扫描面与断言有效性 | P2 | Closed | git history |
| FR-140 | 治理执行成本 — 使其可见、可归因、可预算 | P3 | Closed | git history |
| FR-141 | 持久化层不再交出驱动连接 — 连接能力的 API 边界 | P1 | Closed | git history |
| FR-142 | 触发历史上限从未生效 — 级联删除的决策与修复 | P1 | Closed | git history |
| FR-143 | 变异 fixture 的靶标漂移 — 第三条 meta 断言 | P2 | Closed | git history |
| FR-144 | jq 供给的门禁循环在输入畸形时静默变为空转 | P2 | Closed | git history |
| FR-145 | `producer \| consumer -q` 在 `pipefail` 下是一个假失败——也是一个假通过 | P2 | Closed | git history |
| FR-146 | `producer \| head -N` 在 `pipefail` 下会让门禁中途终止，而截断的运行读起来和完整的一模一样 | P2 | Closed | git history |
| FR-147 | 两个由 CI 执行的 shell 门禁不在执行面清单里，因此每一道派生扫描器都看不见它们 | P2 | Closed | git history |
| FR-148 | 没有任何东西检查 fixture 是否还能被产品接受 | P2 | Closed | git history |
| FR-149 | DD-137 移除了两个构造，19 个 fixture、一道门禁和四份 QA 文档还在描述它们 | P2 | Closed | git history |
| FR-150 | 发布链路完整性修复 | P0 | Closed | git history |
| FR-151 | 0.4.0 版本发布与 Unreleased 清算 | P0 | Closed | git history |
| FR-152 | 首跑路径现代化 — quickstart、fixture 与错误码可读性 | P1 | Closed | git history |
| FR-153 | 供应链与依赖面治理 | P1 | Closed | git history |
| FR-154 | CLI 输出正确性与三文档面一致性 | P2 | Closed | git history |
| FR-155 | 文档与仓库现实对齐 — AGENTS.md、幻觉基础设施、台账再生 | P2 | Closed | git history |
| FR-156 | pipelineVariables 清单授权面退役 | P2 | Closed | git history |
| FR-157 | Source 域分解与测试补强 | P3 | Closed | git history |
| FR-158 | 治理体系自省 — 门禁的门禁、成本与新鲜度 | P3 | Closed | git history |
| FR-159 | 交互会话进程回收 — 孤儿泄漏与 OS 层回收缺口 | P1 | Closed | git history |
| FR-160 | QA harness 的守护进程拆解 —— `wait` 在 23 个门禁里是空操作 | P2 | Closed | git history |
| FR-161 | path-shadow 隔离对登录 shell 下的 provider 解析不成立 | P2 | Closed | git history |
| FR-162 | 失败可见性契约 —— 步骤失败、任务状态与收件箱的三方矛盾 | P1 | Closed | git history |
| FR-163 | 连接、路径与就绪的单源化 | P2 | Closed | git history |
| FR-164 | 审计动作具名化与无信封缺口 | P1 | Closed | git history |
| FR-165 | 账本与契约的驱动化 —— 从"有记录"到"有排程" | P2 | Closed | git history |
| FR-166 | 概念面收敛 —— 双词汇表、重叠 kind 与概念预算 | P2 | Closed | git history |
| FR-167 | Delete 路径的审计缺口——具名与信封双缺 | P1 | Closed | git history |
| FR-168 | Task 删除的引用处置策略 —— 级联清 1/8，其余七表无裁决 | P1 | Closed | git history |
| FR-169 | 数据目录消失后守护进程不自证死亡 —— 22 小时、零日志、服务零客户端 | P1 | Closed | git history |
<!-- END GENERATED FR REGISTRY -->

## 说明

- `P0`: 对安全性、控制面暴露面或系统可信边界有直接影响
- `P1`: 对系统一致性、平台成熟度、生产可用性有显著影响
- `Proposed`: 已形成正式需求，尚未进入实现阶段
- `In Progress`: 已有部分阶段落地，剩余阶段仍在治理中
- `Implemented`: 需求已完成并进入维护阶段
- 已闭环并删除的 FR，应由对应 `docs/design_doc/**` 与 `docs/qa/**` 继续承载设计和验证信息
- FR-127 至 FR-133 源自 2026-07-25 的技术负债深挖，共同特征是**治理编写侧严格而执行侧未接线**：门禁、镜像、同步链路、依赖策略均存在"写了但不跑"或"从未被检查"的缺口。FR-127、FR-128、FR-129、FR-131、FR-132 与 FR-134 均已闭环——执行面已打开并改由执行事实校验，覆盖面改为发现式，既有门禁的维护摩擦已降低，镜像缺口已消除，文档发布链路已单一来源化，CI 每个 job 的存活性进入台账；FR-135 也已闭环（`boundary-coverage` 已首次转绿，`known-failing` 标注移除）；FR-130 的三个 phase 已于 2026-07-27 全部完成，其后继是 FR-141；FR-133 已于 2026-07-28 闭环，依赖策略门禁挂到了该执行面上——并且发现它需要两道而非一道：`cargo deny` 证明策略成立，`scripts/qa/dependency-policy.rb` 证明策略仍然绑定。FR-076 的需求 1（GUI CI 集成）同源，已在该 FR 内单独提升为 P1
- FR-150 至 FR-158 源自 2026-08-01 的全维度技术负债审计（需求→架构→用户体验→工程化，at `9bcfaa96`），共同特征是**核心代码质量高而"外壳"系统性断裂**：发布链路存在两处必然失败的缺陷且 4 个月未发版（FR-150/151）、首跑路径与 agent 入门文档在教运行时已废弃的写法（FR-152/155）、依赖治理存在覆盖空洞与过期自述（FR-153）、CLI 输出格式间数据不等价且约 26 个叶子命令零文档（FR-154）、协调坍缩账本点名的后续 FR 从未立项（FR-156）、Source 域单文件 2500+ 生产行而覆盖率 12%（FR-157）、治理体系自身复现了它警告过的手抄清单反模式（FR-158）。执行顺序与依赖关系见各 FR 的「依赖与关联」节：150→151 为阻塞链，152/153 与 FR-076 需求 1 可并行，154/157 应排在 0.4.0 发版之后，158 收尾
- FR-169 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/185-daemon-data-dir-vanish-self-termination.md` 与 `docs/qa/orchestrator/223-daemon-data-dir-vanish-self-termination.md` 承载。**step 0 实测出一件原文未主张、并直接改变定级的事**：删除数据目录后**重建**同名路径，旧守护进程仍活着且对已 unlink 的数据库持有 7 个打开的 fd，而**第二个守护进程在同一路径上启动并就绪**——脑裂。路径存在性检查在这一格恰好读到「健康」，故检测判据定为身份 `(st_dev, st_ino)` 而非 `path.exists()`；该分界线由实测变异证明：换成存在性检查后四个场景里三个照常全绿，**只有**「删除后重建」变红。据此由 P2 提升为 P1。同时定位了放行第二个守护进程的机制并**明确不在本 FR 范围内**：单例守卫 `detect_running_daemon` 读的是数据目录**内部**的 pidfile，删掉目录就删掉了守卫的全部证据——「当状态目录本身可能消失时，单例状态该存在哪里」是独立的设计问题，另起 FR；本 FR 只把脑裂窗口收敛到约 15s，不消除它。**三处对本 FR 自身文本的更正**（作者与治理者是同一份努力，故逐条对抗性复核）：需求 2 原要求「以可区分的退出码退出」，改为「专属 reason + 具名日志、退出码不变」——本 FR 自己的验收标准早已写明断言诊断而非退出码，非零退出会诱使 supervisor 对着仍不存在的目录反复重启，且改退出码就意味着不共用需求 4 要求的既有序列；需求 4 后半句「清理失败不得记为 error」查明**已经满足**（`lifecycle::cleanup` 两行都是 `let _ = remove_file`，结构上不可能产生日志），要求作废而非「已实现」；需求 3 引 §4.4 shape 11 要求「实测分布再选阈值」**不适用并撤回**——shape 11 的前提是存在一个必须被分开的总体，而这里的 N 是安全余量的重试次数，本地卷上对存在目录 `stat` 不会瞬时失败，照搬只会**制造**一份测量；正确形式是写明理由并承认它是判断（5s×3，容得下两次瞬时失败，15s 远低于任何人或 supervisor 的反应时间）。引用先例必须点明使其可迁移的前提，这正是 FR 作者没做到的地方。裁决：**不新增任何参数**，两值为常量，概念预算净增为零。退出复用既有 `shutdown_notify` 序列（DD-150/FR-168「一条语句序列而非两条」施加于关停）；`shutdown_reason` 的 `data_dir_vanished` 臂**排在最前**：目录消失后 `worker_stop_signal_path` 因其路径就在目录内而读到 false、`shutdown_requested` 又因 watcher 刚置位而为 true，排在后面必然把原因归错。迟滞的计数清零提成纯函数 `observe_data_dir` 以便无需 sleep 断言（用 sleep 写的迟滞测试终将变 flaky 然后被删掉），夹具用「跨越一次恢复的四次消失」对阈值 3 要求不触发，删掉清零那一行只有它会红。**跑通端到端撞出一条本路径必然出现的 `error!`**（session drain 无法查询随目录消失的数据库），降为 `warn` 而非静默：在唯一必然触发它的路径上留一条 error，训练出来的是「跳过 error 行」的习惯——这是读 diff 读不出来、只有真跑一遍才会撞到的东西。已知限制：脑裂窗口被收敛而非消除；只检测整目录丢失，socket 单独被删、数据库单独被删或被截断三种信号未测未处理；真实 `~/.orchestratord` 上的行为是推断而非观测（实测一律在隔离目录进行，运行时数据库不是拿来做测试的）；`slack-gateway` 自有数据库对同类事件的反应未测量
- FR-168 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/184-task-delete-reference-disposition.md` 与 `docs/qa/orchestrator/222-task-delete-reference-disposition.md` 承载。**裁决（本 FR 的唯一交付物）**：七张阻塞表按所有权二分——`handoff_snapshots`、`resume_plans`、`source_bindings` 随任务删除，`resume_executions.child_task_id`、`source_events.routed_task_id`、`source_routing_attempts.task_id`、`source_automation_routes.task_id` 保留行并置空引用。可空性在裁决**之前**从 schema 逐列派生，结果与所有权完全对齐（三张 NOT NULL 全是任务拥有的行，四张可空全是独立记录），故 schema 早已隐式编码了答案；该对应关系被断言而非假定。`source_automation_routes` 是两个候选答案分歧最大的一张：它携带 `UNIQUE deterministic_task_id`，删行会释放该幂等键令重放重新触发，审计论据与正确性论据同向，故并非近似判断。retention 与显式 delete 取同一答案（处置下沉进共用例程），`--force` 保持纯确认闸门、**未**获得改变处置的权力。**设计的形状**：引用集合每次删除都从 schema 派生，裁决集合写死，派生到而未裁决的一律 block-and-report——组合失败关闭，后加的表拒绝删除并点名自己。**step 0 推翻或换据五处**：`PRAGMA foreign_keys` 原引的是 `#[tokio::test]` 里的断言而非启动断言（生产启用点 `sqlite.rs:22`，结论不变）；「七表均有生产写入路径」的论据（`grep INSERT`）测的是错的量——三张经 UPDATE 而非 INSERT 获得引用，`source_automation_routes` 的 INSERT 写的是 `deterministic_task_id` 这**另一列**，逐列重导出后结论成立而论据全换；需求 3 的前提为假——三条路径**早已**共用 `delete_task_and_collect_log_paths`，无重复可消，分歧在例程之外，需求遂改为把处置下沉进例程本身；QA 188 是 5 个场景不是 4 个，且其场景 2 的前提被本 FR 作废（`resume_plans` 不再扣住任何东西），改由测试内新建的未裁决表驱动——这反而是更强的夹具，因为它同时证明阻塞集合确由运行时派生；来源 ticket 已被 `cd61e39b` 删除。存活的 claim 双路径重导出：10/2/8/1/7 的引用面（snapshot 的 `pragma_foreign_key_list` 与 `migration_steps.rs` 的 `REFERENCES tasks(id)` 各数一遍，均为 10，其中 2 条带 CASCADE），七表点名无误，且缺陷**逐表**复现——七张全部以 `FOREIGN KEY constraint failed (19)` 拒绝，原文只演示了一张。**本轮最贵的一课在测试自身**：本套件第一版**根本不可能失败**——它遍历实现所用的映射表并断言行为与记录值一致，于是翻转一条裁决会连预期一起翻转；把 `source_events.routed_task_id` 从 null-the-reference 改成 delete-with-task（静默摧毁入站审计行）通过了当时全部五个测试。该断言看起来是派生的，也确实是派生的——派生自被测对象本身。§4.4 的规则是「从台账派生预期，绝不复述」，而此处的陷阱是**判断没有台账可派生**：schema 与代码树都无法被问出「一行意味着什么」。凡此为真处，设计记录**就是**台账，测试必须复述它，好让改一条裁决需要动两个地方而其中之一明说正在改设计决定。修法是两道彼此独立的检查（裁决的机械回声 + 派生出的可空性属性），对该变异给出互异诊断。同源错误在事实采集阶段也犯了一次且给出的是自信的错误答案而非谜团：首版复现脚本对七张表全部报告「删除成功」（与真相相反），因为它把硬编码的 SQL 字符串字面量当结果行输出，真错误走 stderr 被 `tail -1` 丢弃——§4.4 shape 1 与 shape 5 的合体，出现在核验方手里。**顺带修掉三处既有缺陷**：`cleanup_old_tasks` 此前对任何错误 `?` 直接中止整批，于是一个删不掉的任务会让排在它后面的每个任务永远清不掉（该轮如此，之后每一轮亦然，因为清扫每次都撞上同一行）；`delete_task` 此前先停运行时再发现被拒，把操作员留在「既没运行也没删掉」的状态，现改为先查引用；`anyhow::Error` 转 `Box<dyn Error>` 会**丢弃具体类型**（实测而非假定），故引入 `CarriedError` 承载，让调用方能问「这次拒绝是不是引用扣住」而不必匹配消息文本。**已知限制**：频率仍未测量（七张全部可达已逐列派生，但真实任务各积累哪几张、多频繁未知，裁决后此问题的紧迫性大幅下降）；其他父表（project/workspace/agent 的删除）未清点，`blocking_references()` 按 `tasks` 硬编码，形状可泛化而裁决不可；失败关闭的默认分支在生产中当前不可达——七张全部已裁决，故 block-and-report 只由新建表的测试驱动，即预期稳态，但操作员会撞上的那条分支是由夹具而非由使用证明的
- FR-167 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/183-delete-audit-completeness.md` 与 `docs/qa/orchestrator/221-delete-audit-completeness.md` 承载。**step 0 重建 FR 事实，五条更正，其中三条改变了工作内容**：墓碑 author 是 `daemon-delete`/`project-delete` 而非原文的 `daemon-apply`，且所引行号指向 `load_config` 而非墓碑写入（结论"硬编码、不可归因"不变）；原文第 2 条**低估了自己**——被丢弃的信封不是假想客户端的，而是 CLI 每次删除都发送的那一个（`crates/cli/src/commands/resource.rs`），默认路径就是被丢弃的那条；`RuntimePolicy` 事实上不可删除（`canonical_project_kind` 无该分支），故"12 kind 各有具名删除动作"不可能是 12 次成功；删除面比 12 个 kind 更大——`crd`/自定义资源同样可删且同样零审计行，原文未提及；`core/src/resource/parse.rs::delete_resource_by_kind` 无任何生产调用方却携带 13 分支别名表，是 FR 自己所述"倒置的退役目标"形态出现在它所讨论的文件里。治理顺序一节的 FR-166 依赖已解除：FR-166 闭环且 `ResourceKind` 未变（DD-182 决定 2/3），且其反向要求已兑现——两条决定都显式写出"审计动作名是下游消费者、已记录的动作名永不重命名"，故三项需求一并治理。三项永久裁决：SourceTaskTemplate 取 `source.template.delete` 而非规则字面的 `resource.source_task_template.delete`（取"一 kind 一族"，否则查一个模板的一切需要两个前缀，且查其一会静默只得一半），由 `apply_and_delete_share_a_family` 断言而非托付于自觉；RuntimePolicy 断言一行 `status=failed`（信封在执行前预留，删一个 runtime policy 的**尝试**正是审计存在的理由）；enum 之外的 kind 记通用 `resource.delete`/`resource_manifest`，与 apply 侧同形，只新增一个名字而非两个。`apply_target_type` 更名 `resource_target_type` 由两个动词共用——它命名对象而非动词，且 delete 侧既存的三个取值本就与之一致，故无一行迁移，而第二张表随时可以漂移且漂移无声。**写行为断言时撞出两个既有缺陷**：`delete_resource_from_project` 把 SecretStore 别名并进 `proj.env_stores` 与 EnvStore 共用一条 match 臂，而两个 kind 是两张 map——于是 SecretStore 根本删不掉（对存在的 store 报 not found），且同名 EnvStore 存在时删掉的是**它**，而 `canonical_project_kind` 返回 `SecretStore`，墓碑行、删除守卫与 resource 行删除三者都点名了一个没被碰过的 kind；dry-run 存在性检查带同一处混淆，且根本没有 ExecutionProfile 分支，靠 `_ => false` 把它吞了。两处均无测试，因为生产路径上从没有人删过 SecretStore——而 FR-166 三天前刚把"两者之间搬值是 delete + re-apply"写进指南，那是一条正确的建议配一条做不到的代码路径。修法是改**派发**而非改那条臂：两半都改为按解析出的 `ResourceKind` 无 `_` 通配派发，两个 kind 想共用一条臂就必须有人明写。四张手抄别名表合并为一张（`resource::kind_aliases`，对枚举穷尽），因为两张独立的表失败在不出声的方向——删除侧认识而命名侧不认识的别名，会把资源删掉并记通用动作名，任何日志里都没有一行。三个负夹具逐一实测且诊断互异：恢复旧守卫红 5 项（kind 遍历失败在 `resource.trigger.delete`）、把 SecretStore 塌回通用名红 6 项且每条诊断都点名 SecretStore（遍历失败在 `resource.secret_store.delete`，与前者不同）、把 CLI 信封**注释掉**而非删除红 3 项而 `grep -c` 仍返回 2。收尾清点了一条比两个 FR 都更值钱的事实：daemon 内 `action_audit::begin` 的全部生产调用点中，delete 是**最后一处**条件守卫——把"我们修了两个"变成"一个不剩"。已知限制：`resource.delete` 一名对应三种情况（CRD、自定义资源、解析不出的字符串），靠 `target_id` 而非 `action` 区分；墓碑 author 仍是常量，与审计行的真实 actor 分属两表、只能按时间戳关联；`delete_resource_by_kind` 记录而未删除；`kind_as_str_covers_all_resource_kinds` 名不副实的问题自 DD-177 起仍在
- FR-166 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/182-concept-surface-convergence.md` 与 `docs/qa/orchestrator/220-concept-surface-convergence.md` 承载。**step 0 重建 FR 事实，四条未通过，其中两条改变了工作内容**：Trigger 是四职而非三职（`TriggerEventSpec` 另带 `webhook` 与 `filesystem`），且原文所引 `02:441-452` 属 `## 12. SourceTaskBinding` 而非 `## 10. Trigger`——按原文裁决"三职"对 filesystem 一职不产生任何决定，这正是 step 0 要找的 category conflation；EN TOC 直链中文的是 5 行而非 1 行，需求 2 按原样只关闭五分之一并报告成功。另两条：`"Wish"` 大小写敏感为 0 但 `zh/08` 有两处小写 `wish`，即未定义词已进入用户文档（结论比原claim更强）；"CLI 改词波及 127 命令面"高估约九倍（`task` 下仅 14 个 leaf），论据已替换而非重复。四项裁决三项为"不改代码"：EnvStore/SecretStore 不合并（kind 实为加密落盘、导出脱敏、六个 `secret key` 叶命令三处行为的开关，而 `rg -i 'encrypt|rotat'` 在 guide 02/05 命中 0——读者此前无从选择）；Trigger 不拆（`triggerRef` 依赖其持有 installation 身份与 actorRoles）；`type:` 不强制（改为补上缺失的第三条推断规则：任意未知 step id 回落 `required_capability = <id 原文>`，故 `id:` 笔误不是校验错误而是稍后别处才发现的另一种需求）。Console 改说 Task 只动表现层，`#/processes`/`#/new-process` 仍可解析，刻意不动路由判别式、`wish-pool` 线值与产品名；两个替换陷阱以断言而非假设处理（进程在中文兼指守护进程，全量替换满足全部词汇断言却把 daemon 诊断改成另一问题的建议；nav 标签与 href 从同一渲染元素读取，只改其一都能通过只读另一半的断言）。两个新门禁均派生而非列举：`check_resource_kind_catalog` 读 `ResourceKind` 与散文对差（原列表漏三个 kind、多一个 CRD，此前无任何检查连接二者），`check_guide_language_parity` 靠**测量**找中文文档——字符比无法分离（技术中文 Latin token 密集，四篇 runbook 为 0.44~0.52 而半双语英文章为 0.15），改按中文散文行占比得 0.94~0.97 / 0.37 / 0.00。四个新夹具双向且断言诊断本身而非仅退出码。概念预算规则入 CONTRIBUTING、orchestrator-guide skill 与本 skill 的authoring contract，**刻意不门禁化**："这个概念是不是伪装的参数"是判断题，门禁只能检查"存在某段说明文字"——§4.4 shape 1 的文本存在性代理。已知限制：词汇断言是逐键正向的，新写的旧词汇字符串抓不到（§4.4 shape 2 作用于本 FR 自己的测试）；语言阈值是实测标定而非原理推导，今日边距 0.37~0.94；四篇 Slack 中文 runbook 逐条记入 `translationGaps` 后仍未翻译；`agent-driver-model.md` 37% 中文行的混排问题未处理
- FR-165 已闭环删除；其四项需求的设计与验证信息现由 `docs/design_doc/orchestrator/179-ledger-driven-manual-gate-freshness.md`、`docs/design_doc/orchestrator/180-forward-only-rollback-contract.md`、`docs/design_doc/orchestrator/181-two-sided-ratchets.md` 与对应的 `docs/qa/orchestrator/217-manual-gate-freshness-enforcement.md`、`docs/qa/orchestrator/218-forward-only-rollback-contract.md`、`docs/qa/orchestrator/219-two-sided-ratchets.md` 承载。四个失驱动面各自接上了驱动：需求 1 把新鲜度判据从"记录够新"改为"上次运行 exit 0 且树干净"并把 release 工作流做成唯一强制点（其 `--wait-ready` ticket 由 `77cc351a` 修好后四个 Slack 门禁一并转绿，台账 not-fresh 23→10→6，六条全 never 且三条已具名豁免，实际阻断发布的只剩三个需常驻 daemon 的门禁——该形态决定不属本 FR）；需求 2 把回滚契约收敛到 `crates/orchestrator-persistence/src/migration.rs` 一处，第二句子契约（上一 release 二进制必须能服务当前 schema）由 schema 超集断言强制，散文由逐处记账的门禁盯住（`forward-only` 实为四义而非 FR 原文的三义，第四义与 A 类在同一张表相隔四行，故必须逐处而非按路径）；需求 3 把覆盖率棘轮做成双边——单边棘轮不只是不奖励改进，它会丢掉自己存在的那个用途：CLI 批准值 35.49% 而实测 53.02%，那状态下 CLI 掉 17 个点门禁仍绿。宽容带 3.0 点由实测分布派生（22 对指标里 12 对漂移恰为 0、四对在 +0.95~+1.25、两对在 +4.87~+5.17、四对在 +10.88~+24.12，(1.25, 4.87) 是空区间），5.0 被否是因为它会把 daemon/session 的两个指标劈到带的两侧；需求 4 给 audit.toml 的 18 条接受加上逐条可机读的退役条件并对 Cargo.lock 强制，其中 `patched>=` 形式正是纯在树性检查看不见、且 FR-133 记录过 `--deny unmatched-skip` 在 deny 一侧同样漏掉的那一半。闭环过程中另修掉两个自需求 1 起就红着的 ci-required 门禁（`fixture-target-drift.rb`、`test-governance-ledger-tooling.sh`，根因是需求 1 的认证用了手写清单而非从 enforcement manifest 派生），闭环后两项遗留也已收尾：`ci-liveness.rb` 经推送与两轮收敛刷新完毕（15 个 job、0 known-failing，`af672d21` 的 CI 全绿）；三个 never-run 门禁两转一豁免，`--strict` 首次退出 0。仅 `scripts/qa/test-markdown-link-integrity.sh` 在 macOS 上的 SIGABRT 仍未定位，记于 `docs/ticket/20260811-markdown-link-gate-aborts-on-macos.md`——本次会话曾为它重复开票并写下一个被该旧票证据否定的结论（归因于被忽略的构建产物），重复票已删、结论已撤回，旧票领先的假设（每文件一次的 BSD awk）本次并未真正测试
- FR-150 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/161-release-pipeline-integrity.md` 与 `docs/qa/orchestrator/199-release-pipeline-integrity.md` 承载（publish 列表补 `orchestrator-persistence`/`orchestrator-slack-gateway` 并由 `scripts/qa/test-release-publish-surface.sh` 从 cargo metadata 派生断言集合与拓扑序；Intel macOS 决策为不支持——install.sh 与 formula 在触网前显式拒绝并给出 `cargo install` 替代，行为级验证带 curl 哨兵；formula license 修正为 MIT；tap 推送改为内联 `gh`/`git`,消除持有 `TAP_GITHUB_TOKEN` 的 moving-branch 第三方 action；musl 记录为"门禁但不出货"的书面决策。真正的 publish 端到端由 FR-151 的 0.4.0 发版闭环,QA 199 场景 5 显式记录该边界）
- FR-151 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/162-release-0-4-0-phantom-reconciliation.md` 与 `docs/qa/orchestrator/200-release-0-4-0.md` 承载（0.4.0 已真实发布：12 个 crate 全部上架 crates.io、GitHub Release 三平台产物齐全、Homebrew tap 更新并实测安装、install.sh 在 macOS arm64 与 Linux x86_64 各验证一次。幻影 v0.3.1 根因查明为多 tag push 事件抑制——原 GITHUB_TOKEN 假设被证伪，38 个远端 checkpoint tag 已清理，发布规程固定为单 tag 推送；新增 ci-required 门禁 `scripts/qa/test-release-tag-liveness.sh` 使下一次幻影当天可见。发布实测暴露并修复了 orchestratord 越出 crate 根的 include_str!——publish-surface 门禁新增第 4 项派生检查防止复发）
- FR-157 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/170-source-domain-decomposition.md` 与 `docs/qa/orchestrator/208-source-domain-decomposition.md` 承载（需求 1：`daemon/source_connection` 行覆盖 12.21%→**85.51%**（303/2482→2089/2443，macos-aarch64 / cargo-llvm-cov 0.8.5 @ `497339b9`），三路推导互证——门禁自身的 `summarizeRust` 与独立选集同为 2089/2443，LCOV `DA` 记录以不同计量单位得 2029/2335=86.90%；分母由 2482 降至 2443 是模块的 75 行内联测试迁入 `tests/` 所致，而分母缩小恰是覆盖率虚高的唯一制造方式，故实测而非解释：按旧分母同一分子仍为 84.2%。12% 的成因不是难测而是**从未进入**：`BoundaryFixture` 传 `slack_gateway: None`，绝大多数 handler 在网关未配置分支即返回；新 fixture 用一个 axum app 同时提供网关协议 `/v1/**` 与 Slack manifest API `/api/apps.manifest.*`，`SlackGatewayClient::new` 的 loopback 例外使其无需 TLS 即可到达 OAuth intent 生命周期、shared↔dedicated 迁移 fence、含 HMAC 导入回执的 dedicated 审批、权限扩张与非扩张两种清单升级、App 删除、断连与所有权转移。stub 记录每次请求的 path/body/bearer，因此断言观察的是某端点确实被以正确凭据访问，而非 handler 返回了 Ok——后者对静默跳过网关调用的实现同样成立；未配置路径回 503 而非 404，使实现触达测试未预期的端点时高声失败而不是走进一个像合法错误的 not-found 分支。需求 2：2572 生产行拆为六个模块（`mod.rs` 84 / `query.rs` 154 / `dedicated.rs` 631 / `oauth.rs` 572 / `lifecycle.rs` 506 / `transfer.rs` 295 / `projection.rs` 382），**纯移动并被证明为纯移动**——仅归一化拆分强制的四处机械调整（两处父模块路径、清单资产 `include_str!` 深度、跨模块 helper 改 `pub(super)`）后，前后正文行的多重集完全相同：2498 行，零新增、零删除、零修改；尺寸上限由遍历目录的 cargo test 持约而非手抄模块名，并另配是否真的被分解的断言，因为单个 999 行文件同样满足尺寸检查。需求 3：proto 面出**决策而不动线上**——保留单 `service OrchestratorService`（拆分会使客户端存根与挂载代码倍增，以换取模块拆分已在痛处解决的文件尺寸问题），写入新增 RPC 的域归属规则（资源前缀、子资源嵌套、15 个无前缀 daemon 动词的封闭集、消息命名派生律）；三个真正封闭的字符串枚举维持字符串——转 proto enum 需换字段号，而全文件 **0 处 `reserved`**，即没有既有的字段号退役纪律，代价是三个版本的双写，换取一个 Rust 侧唯一边界已强制的属性；改变该决策的条件被写死：出现 Rust 工作区之外的第四个 `action_audit_mode` 读者。需求 4：三个词各收敛为单一定义点——**常量落点与 FR 原文相反**：core 依赖 orchestrator-config 而非反向，模式常量置于 `orchestrator-config` 经既有 re-export 触达全部调用点，core 公共面维持 128 文件 / 49 `pub mod` / 611 项，**计划中预算的 ledger 再生并不需要**；并配两条行为断言，因为字面量计数只证明拼写变了、不证明落进审计表的值没变：无审计上下文的变更仍以 `"legacy_client"` 记录（断言对象是线上一直承载的字面量而非常量本身）并带合成的 `legacy:<request-id>` 幂等键，而在真实 `action_audit_mode: enforced` 的 `RuntimePolicy` 下同一调用被拒、显式 reason code 仍被接纳）。**规划核查推翻了 FR 原文的四项事实**：`source_router.rs` 的 1574 是生产行、该文件总计 2275 行含 701 测试行并不欠测试；`"legacy_client"` 是 20 处 / 7 文件而非 17 / 6，漏掉的 `server/action_audit.rs` 里它是比较而非赋值；`"compatibility"` 是 10 行 / 11 次 / 6 文件且跨 core 边界；三文件占 daemon 生产代码 31.9% 而非 28%，`*source*.rs` 剔除 glob 同时命中的 `resource*.rs` 后为 18415 行。并补上原文没有的一项：`coverage-governance.mjs:121` 把 keyModule 钉死在精确 `.rs` 路径而 `matchingBucket` 用 `startsWith` 前缀匹配，**需求 2 的拆分会静默掏空需求 1 被计分的那把尺**——门禁不会失败，只会在近乎空的分母上报出更高的百分比，即 §4.4 shape 2 落在测量装置自身；现 keyModule 分别点名文件与目录——仅去掉 `.rs` 后缀只在一个方向上精确，裸前缀会把任何以该串开头的同级文件一并吞入；fixture 守的是被测量的集合而非路径拼写（含一个子模块、拆分前的单文件、`tests/` 下 100 行测试源、以及一个仅名字前缀相同的近似同级文件，断言该模块为 12/15 行；三个变异各以不同数字失败——裸前缀报 65 行、仅 `.rs` 前缀报 5 行、去掉 `/tests/` 排除使组件变为 97.14%，三者都实跑过）。词汇表门禁的作用域与豁免同样是派生的（命名 `action_audit_mode`、`fallback_reason_code` 或 `reason_code` 的文件才进入——第三个标记是闭环自检加的：仅用两个精确字段名只覆盖今日的树，对以其它字段名写 reason code 的新生产文件会失明，加入后考察集由 19 个文件扩至 52 个而门禁依然通过；测试代码经三种派生方式豁免），三个变异各以不同诊断失败，其中一个专防扫描器在零输入上报成功；第四个变异被正确忽略——它命中的是 `core/src/resource/project.rs` 自身 `#[cfg(test)]` 之后的行，门禁没错、变异选错了，此事留档是因为门禁未触发与门禁已坏在日志上完全一样。另记录三项本 FR 不修的边界：`dedicated_preview` 的评审后路径在进程内不可达（评审用清单必须携带公网 HTTPS 端点，loopback origin 必被 `render_manifest_endpoints` 拒绝——本可加 `#[cfg(test)]` 缝隙绕过且仓库有先例，但那等于关掉被测的安全属性，故改为把该拒绝本身断言为行为，并以播种会话到达其后的审批流）；`server/source.rs` 仍是 1433 生产行 / 0 测试行；覆盖率基线的非 source 条目欠棘轮且可量化——CLI 实测 52.86% 而批准值仍是 35.49%（分母 6038→7373），因为比较是单向的，这些条目会一直通过下去，正是 2026-07-27 再批准注记已点名、现在有了尺寸的那个缺口
- FR-152 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/163-first-run-path-modernization.md` 与 `docs/qa/orchestrator/201-first-run-path-modernization.md` 承载（README quickstart 改为可复制执行——`fixtures/manifests/bundles/quickstart.yaml` 随仓库提供并被 corpus 测试永久收编，干净环境全程零 `[legacy_*]` 有记录实测与 Rust 层永久断言双层保障；fixture 语料 118 个 Agent document 迁移 typed driver、7 个按 `# fixture-driverless-exempt:` 约定豁免、防回潮门禁 scope 派生自 git ls-files；错误码词汇表 EN/ZH 16 码由三规则源码派生双向比对进 ci-required，CLI apply 输出附 `orchestrator guide error-codes` 指引；install.sh skills 解包改为显式公告目标目录并以文件系统级断言防回归。核查修正了 FR 自身的两处事实错误：错误码实为 16 个而非 10 个——字面量 grep 看不见 driver_error 插值形态，guide 覆盖也非零)
- FR-154 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/167-cli-output-render-chokepoint.md` 与 `docs/qa/orchestrator/205-cli-output-doc-parity.md` 承载（需求 1：json/yaml 分歧从结构上不可能——每载荷单一 `serde_json::Value` 经 `output/render.rs` 唯一咽喉点序列化,`format_parity` 22 个满填充载荷深度相等 + 比对器自身可失败性证明 + 咽喉点源扫描三层保障,16 处 `unwrap_or_default` 吞错全数改为 stderr+非零退出,6 处广告 yaml 却运行时拒绝的 bail 删除；需求 2：全量 `-o` 约定落地（21 个硬编码格式的 mutation 补上 `-o`,流式命令改 `StreamFormat {json,yaml}` 使 table 从 --help 诚实消失,`--json` 隐藏别名保留一个周期,破坏面全量进 CHANGELOG Compatibility）；需求 3：clap 树落库为 `cli-surface.json`（cargo test 字节级保鲜）,内置 guide 90→127 条与 clap 双向集合相等,EN/ZH 参考补齐 63 叶并由 ci-required 门禁 `test-cli-doc-parity.sh` 按叶覆盖+无效命令拒绝+guide 等式三面持约,6 个 fixture 各自点名派生受害者。核查修正了 FR 自身三处事实：零文档叶为 29 非 26（漏 `source binding` 3 叶）,guide.rs 为 1802 行,且存在两处比原文更严重的分歧——`event list` yaml 改名改型 `payload`→`payload_json`、`agent session` yaml 为手写伪 YAML 丢 17 字段中的 12 个）
- FR-156 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/169-pipeline-variable-surface-retirement.md` 与 `docs/qa/orchestrator/207-pipeline-variable-surface-retirement.md` 承载（step 级 pipeline 变量授权面——`store_inputs`、`store_outputs`、`step_vars` 与 `store_put` 后置动作——在 apply 期以 `[legacy_pipeline_variables_removed]` 拒绝并点名字段，校验递归进 `chain_steps`；两个生产消费者改为以 `orchestrator store get ... --project {project_id}` 自取，新增 `{project_id}` 上下文模板变量；`consumerInventory.pipelineVariables` 迁至 `removed`/0 并新增 `retainedCarrier` 记录 `PipelineVariables` 为**保留载体**而非欠账，`sourceBaseline` 经 `--emit-baseline --write` 由 30 降至 29，新增的 `--emit-consumers` 补上了该 FR 验收标准要求却不存在的计数再生路径）。**规划核查推翻了 FR 原文六项事实中的四项**：计数器统计的是清单触点而 4 项 blockers 是 Rust 面，二者无算术关系且其中一项本就是清单消费者而非代码阻塞项；退役目标倒置——`PipelineVariables` 是 `PreservedExecutionChannels`（全仓 3 处引用、全在 `pipeline.rs` 内）的唯一载体，死的是清单授权面；30 行全是该存活载体的 import 与签名，迁移移除 0 行；验收标准 1 在 `--emit-consumers` 之前不可满足。**§4.3 的 pre-migration baseline 抓到两个纯结构检查看不见的缺陷**：`promotion#gather_updates` 的占位符哨兵 `[ "$LAST_SHA" != "{last_published_sha}" ]` 因模板替换是全局的而恒为假，其"自上次发布以来的变更"分支从未执行过；`store put` 与 `store get` 并非互逆——写入裸字符串后读取报 `failed to parse stored JSON value`，而 `promotion.yaml` 一直这样写 SHA。另发现网关自身的两处枚举缺陷：`store_put` 后置动作是无人计数的存活消费者，而 `outputs`/`pipe_to` 从未接线（不是 `WorkflowStepSpec` 字段、所有转换路径恒置空）却被当作生产消费者计数，已随 `pipe_to_unknown` 检查一并删除。已知限制：步骤内的 `orchestrator` 调用需能发现自身 daemon，runner 的默认 env allowlist （`PATH, HOME, USER, LANG, TERM`）仅在 daemon 位于同一 `HOME` 下的默认数据目录时足够，否则需补 `ORCHESTRATORD_DATA_DIR` 与 `ORCHESTRATOR_CONTROL_PLANE_CONFIG`——这是相对被移除的进程内绑定的一处退步，已在 DD-169 留档而非隐藏
- FR-155 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/168-docs-reality-alignment.md` 与 `docs/qa/orchestrator/206-docs-reality-alignment.md` 承载（`AGENTS.md` 的所有 YAML fence 经真实 parse/validate/warning/apply 路径执行，治理中额外抓到并修复了 Workflow 缺必填 `type`；architecture 区分 `gui/` Web 与 `crates/gui/` Tauri、补 persistence/Slack/四个二进制并从 Rust 派生连续 37 迁移；29 个 skill 保持可发现，路径候选以精确 scope 双向校验且 `SKILLS.md` 生成化，6 个基础设施 skill 仅在目标资产存在时激活；active ticket 改为入库、验证后删除、git history 归档；FR registry 从完整 HEAD 祖先派生 140 编号/145 路径，13 条史前例外与 5 个编号碰撞显式留证，浅克隆拒绝；61-RPC 根 proto 删除并统一到 121-RPC crate 正本，5 个散落 YAML 与临时 exclusion 删除，行为由 canonical build、QA-105 fixture 与单元测试承载。规划核查修正了原审计的 30→29 skill、10→11 非 FR proto 引用、手抄缺口→完整历史边界等事实；闭环派生 sweep 又抓到首版 reality fixture 的两个 mutation 未证明落地、一个 `abort` 可截断汇总及两处 bash 3.2 空数组展开风险，现分别由 `gate_fixture` 与 guarded expansion 接住；post-closure 验收再发现 AGENTS 新引入了全历史不存在的 `quality-gates.json`，且 `gui/` 子串断言会被 `crates/gui/` 满足，现以 AGENTS 路径存在性派生、Web/desktop 精确语义断言、两条独立 mutation 与 registered-check target meta 共同闭合）
- FR-153 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/164-supply-chain-dependency-governance.md` 与 `docs/qa/orchestrator/202-supply-chain-dependency-governance.md` 承载（依赖面的自述全部改为派生事实：npm 三树（含 portal 模板，原 FR 漏计）重回 Dependabot——此前 `3446b652` 无记录整体移除九天无人察觉——新增 `dependabot-npm-coverage` 规则从仓库遍历 package.json 双向断言覆盖集；`deny.toml` 自述数字由 `prose-counts-derived` 门禁化，首跑即抓到 copy 计数过期一天（base64@0.22.1 使 70→71），而原 FR 的裸 lock grep 量错了宇宙——lock 记录从未启用的 optional 版本（serde_with 的 schemars、yasna 的 bit-vec），cargo-deny 检查的 feature-resolved graph 恰为自述的 48；17 项 unmaintained advisory 逐 ID 入账 `.cargo/audit.toml` 并以 `--deny unmaintained` 使账本具备约束力，根因按 11 gtk-rs / 5 unic-via-urlpattern / 1 paste-via-cel-interpreter（daemon 侧）三分，各带 `cargo tree -i` 退出条件；action 版本收敛至被关闭 Dependabot PR 的目标版本——关闭抑制重提议即漂移根因；`.cargo/config.toml` 的 USB 硬盘限速移出 track，`Rust test` 前后实测 259s→252s（Δ −2.7%，噪声内）。副产物：`skip-is-live` 的 lock 幻影窄门记入 DD-164 known limits）
- FR-137 已闭环删除；其**由解析 workflow 得出聚合覆盖面**的判定（对全部 workflow 的全部 job 生效，规则内不写任何 workflow 路径或 job 名）、吞掉失败的步骤消失的**三个方向**各自的负向 fixture（无 `id` 者从构造上不可聚合、有 `id` 而无人读其 `.outcome` 的遗漏方向、读一个不存在 step id 的悬空方向），以及把真实 `Governance result` 脚本抽出执行以证明"被引用"确实承重的三条行为断言，现由 `docs/design_doc/orchestrator/149-governance-aggregation-completeness.md`、`docs/qa/orchestrator/187-governance-aggregation-completeness.md`、`scripts/lib/workflow_model.rb`（`continue_on_error_steps`／`outcome_references`／`outcome_facts`）与 `scripts/qa/test-qa-gate-surface.sh`（`check_continue_on_error_aggregated`，门禁自 12 项增至 13 项、fixture 自 27 条增至 33 条）承载。
  FR 原文的四处事实偏差：计数"20 个 id / 20 条 `OUTCOMES`"实为 **22 / 22**，而本 README 第 98 行当时写的 21 也是错的——该清单历经 **19 → 20 → 21 → 22** 四个 FR 周期各长一条，而描述它的三份文档各自停在不同数字上，这一漂移本身比它试图陈述的数字更能支持该 FR 的论点，故写入 DD-149 而非当作笔误抹去；需求 1 限定"**带 `id:` 且** `continue-on-error: true`"会**放过它自己描述的复现**——无 `id` 的吞掉失败步骤从构造上就无法被聚合且对该检查完全不可见，而这恰是更易犯的那一种（`id` 只在有人已打算读 outcome 时才会写下，忘 `id` 与忘 `OUTCOMES` 是同一次疏忽），故规则改为 `continue-on-error` ⇒ 必须有 `id` ⇒ 该 `.outcome` 必须被读；反向断言的**论证是反的**——FR 称悬空引用"永远解析为空、其效果与遗漏相同"，把真实聚合脚本抽出实测:空 outcome 退出码为 **1**，job 永久变红且指名一个已不存在的门禁,是**响亮**而非静默,与遗漏方向正好相反,规则保留而理由重写,并由行为断言钉死;非目标"不扩展到其他 job"若照办就要把 `ci.yml` 与 `governance` 写成字面量,正是该 FR 要消灭的枚举面本身,实测全仓库仅 `governance` 一个 job 用到 `continue-on-error`,故泛化版零成本通过,依 DD-145 为 FR-134 非目标所立的先例采纳泛化并留档
- FR-138 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/152-shell-lexical-state.md`、`docs/qa/orchestrator/190-bash32-scanner-lexical-state.md`、`scripts/lib/shell_lexer.rb` 与 `scripts/qa/test-bash32-lexer.sh` 承载。缺陷属实：`bash32-compat.rb` 的 `code_lines` 逐行重置引号状态，跨行引号区内的 `<< WORD` 形近物被当作 heredoc 开启符，其后整个文件退出扫描且无诊断；触发第二处的正是 case 9——那条"证明 CI 中存在跑本门禁的 macOS 宿主"的防空转断言，其 ruby 数组追加记号 `<<` 让门禁看不见自己最后 16 行。但 FR 正文有三处未通过实测复核：（1）计数漂移——受追踪 shell 文件 95 实为 **98**，`test-qa-gate-surface.sh` 的逃逸起点 900 / 被吞 252 行实为 **993 / 360**，需求 2 的范围 37 处 / 14 文件实为 **43 处 / 16 文件**（FR 自称 37 是下界，该判断正确）；（2）"当前是潜伏的、没有真实危险构造被藏住"**已不成立**——被吞尾部含 `test-qa-gate-surface.sh:1307` 的裸 `"${TARGETED[@]}"`（`TARGETED=()` 在第 876 行），即本门禁存在的那一类，它不炸 CI 只因该脚本仅在 ubuntu-only 的 `governance` job 中运行；（3）**最关键**——FR 把两处逃逸都归因于"跨行引号状态缺失"，而按字面实现（`in_single`／`in_double` 跨行保持）实测**只修好第一处**，`test-bash32-compat.sh:369` 依旧被吞：第 359 行 `MACOS_JOBS="$(ruby -e '` 先开的是**双引号**，`$( )` 内部引号上下文是重置的，两个布尔量的跟踪器把那个 `'` 判为普通字符从未进入单引号态，随后在第 366 行 `"#{runners} #{matrix}"` 处因 `#` 前是空格而把整行余部当注释丢弃、引号奇偶性彻底错乱。故 `$( )` 须建模为嵌套引号上下文——FR 自己的验收标准"全部行进入扫描"不建模它就无法达成；FR"需求 1 第二条会抓到两处"的判断对但理由不对：在 FR 所述设计下它是唯一能抓到第二处的东西。三行实测台账（18275 行 / 0 finding → **18629 行 / 1 finding** → 18629 行 / 43 finding）中间那行是承重的：两个数字同向移动才把"修好了缺陷"与"换了口径"分开。另需求 2 采推荐路径**放弃 emptyable 推断**，一次消除跨 source 漏报与 DD-146 已记录的流不敏感过报；需求 3 的 `!` 加入 / `not` 移除实测零新增误报。覆盖面证据是逐文件行数台账而非门禁绿灯——缺陷正是在门禁绿灯下发生的；其中 heredoc 行数由词法器丢弃时**计数**而非以 `total - scanned` **推导**，推导版对"在第一行就放弃的词法器"同样成立，会把一次从未执行的核算认证为证据（该错误在治理中被发现并改正）
- FR-142 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/150-trigger-history-limit-cascade.md` 与 `docs/qa/orchestrator/188-trigger-history-limit-cascade.md` 承载。缺陷属实且比 FR 所述更重：`historyLimit` 不是「对跑过的 task 无效」，而是**对任何 task 都从未生效过**——`resolve_task_targets` 五个分支各自都至少产出一个 item path，且与 task 行同事务写入，故每个 task **自创建起**即持有 `task_items` 行，裸 `DELETE FROM tasks` 每次都被外键拒绝。FR 正文有四处未通过实测复核：（1）「全库仅 2 处 CASCADE，都不在这条链上」两处皆误——`grep -c` 数的是行不是处，实为 3 处分布在 2 行，且**三处全在 `tasks(id)` 这条链上**（`task_graph_runs` 与 `task_graph_snapshots` 由 SQLite 自行清除），故会拒绝删除的是 **8 张**而非 10 张；（2）「正确的级联已经存在」不成立——`items.rs` 只清掉 8 张中的 1 张（`task_items`，外加挂其下的 `command_runs` 与本就无外键的 `events`），余下 7 张无人处理，因此 **`task_cleanup.rs` 与 `delete_task_impl` 带有同一缺陷**：用过 handoff、resume 或来源摄入的 task，保留期清理与操作员显式 `task delete` 同样删不掉（已按 `items.rs` 原语句序列实测复现）；（3）用户文档 `02-resource-model.md` 称 `historyLimit` 「默认 5」，而各层皆无默认值，不写即永不清理，且该字段是两个独立数字而非一个；（4）QA 计划所据的「FR-137 未闭环」在立项当日已闭环。A/B/C 三种形态经实测均不可按原文实施（B 需要外键容忍孤儿而 FK 强制开启且三列 `NOT NULL`；C 因每个 task 自带 items 而是永久空操作；A 须扩写 `items.rs` 从而改变 `task delete` 的语义，即 FR 自己的非目标第 3 条），故选定派生形态 **A′**：复用既有事务级联删除执行痕迹，被上述 7 张表中任一引用的 task **整体跳过**并按 `table.column` 上报原因——绝不留下被掏空的 task 行。阻塞集由 `pragma_foreign_key_list` 从 schema 读出而非写死。`config/governance/schema-snapshot.sql` 未变；持久化台账仅 `trigger_state.rs sql 7 → 8`、总计 `514 → 515`。FR 的非目标第 3 条「`task_cleanup.rs` 是对的」已书面撤回，改记为一条由本 FR 发现、但不由本 FR 修复的独立缺陷（见 CHANGELOG 的 Known Non-goals 与 DD-150 的 Known limits）。**这条缺陷是搬迁时被人眼发现的，62 道门禁没有一道能看见它**——它是一条被静默吞掉的运行时错误，记作 FR-140 讨论治理成本时的对照点
- FR-143 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/155-fixture-target-drift.md`、`docs/qa/orchestrator/193-fixture-target-drift.md`、`scripts/lib/gate_fixture.sh`、`scripts/qa/fixture-target-drift.rb`、`scripts/qa/test-fixture-target-drift.sh` 与 `.github/workflows/ci.yml` 的 `governance` job 承载。九次实测全部核对属实（`75dcf68c`、`e6b5fd70`、`ef6f439f`），八次是绿的；最恶劣的一次不只是失效，它报告了一个不存在的缺陷，把审计者引向一道无辜的门禁。FR 原文的三处偏差已在治理期改正并留档：**FR-129 的两条 meta 断言实为两处**（FR-129 在 `test-skill-mirror-integrity.sh`、FR-134 在 `test-qa-gate-surface.sh` 各建一遍），且都以 `ALL_CHECKS` 为键，而**举证的三道门禁一道都没有注册表**——照原文「并列注册」会盖住两道从未漂移的门禁而漏掉三道真的漂移了的；**需求 2 与需求 3 没有存量**（27 道 ci-required shell 门禁上实测各为 0），故作为回归护栏而非修复交付；**真实存量是 21 处未被接住的前提（分布在 15 个 `ruby -e` 块）+ 28 处未证明落地的原地改写，共 49 处、跨 10 道门禁而非 3 道**。附注中「30 余个 Ruby 门禁脚本」实为 16 个。治理期本身又犯了一次同类错误并如实记下：门禁数先写成 11（数了含 0 行的表格行数），第二条路径重算得 9，改用发现式的临时树变量推导后得 10——**枚举缺陷发生在为测量枚举缺陷而写的工具上**
- FR-141 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/151-persistence-connection-capability-boundary.md`、`docs/qa/orchestrator/189-persistence-connection-capability-boundary.md`、`config/governance/persistence-api-boundary-ledger.json` 与 `scripts/qa/{persistence-api-boundary.rb,test-persistence-api-boundary.sh}` 承载。它是 FR-130 Phase B 一次**正确的拒绝**留下的无主项：Phase B 拒绝了"给 `AsyncDatabase` 加闭包方法"的改动（推棘轮而非处置文件，拒绝成立），但那笔交易打包了两件事——推数字，与**持久化层不该把驱动的连接类型放在公开 API 上**（架构边界，从未被单独判断）。
  **治理期七处事实更正**（FR 文件删除后，此处是它们唯一的存续形式）：① 层外调用点实测 **54 处**（core 22 / daemon 21 / scheduler 11），不是原文的 165——原数字写于 Phase B 搬迁之前且含 `cfg(test)` 代码；② 原文的 crate 清单**漏了 `crates/integration-tests`**（`tests/trigger_fire.rs` 5 处 `.reader()`，台账给它 `test-only` 角色的合法消费者），这正是需求 1 自己写下的"枚举式清单只守得住写它时已知的东西"发生在 FR 自己身上；③ 公开面泄漏是 **87 项而非 5 项**，其中 82 项是*索取*连接的 `pub fn(conn: &Connection)` 而非*交出*——需求 1 与需求 4 因此不是同一个范围，任何单一改动都不可能同时满足两条验收标准（category conflation）；④ **改 `AsyncDatabase` 关不上门**：`db::open_conn(path)` 按路径新开连接完全绕过它，层外 27 处生产调用点，两个 forbidden crate 都持有 `state.db_path`；⑤ **`orchestrator-security` 是第三扇门而非局外人**——它的 11 个（原文记 9 个）`pub fn(conn: &Connection)` 是 daemon 那 4 处 `open_conn` 存在的唯一原因；DD-147 因它位于 core *之下*而判 `exempt`，那是关于**位置**的真话与关于**形状**的假话：一个豁免 crate 的 API 索取连接，就是把驱动**向上推过层**、推进那个被禁止持有它的 crate；⑥ `fn other` 是 4 份而非 6 份，其中 2 份在层内、不会也不该消失；⑦ 本 FR 是 DD-147 冻结残量的**偿付方**，而原文对这笔最大的 ledger diff 只字未提。三扇门串联（门 3 → 门 2 → 门 1），已裁决全关；只关门 1 会让新门禁在自己的 §4.4 反问上失败——"这条断言还会在什么坏状态上通过？"答案是"今天这个状态"。
  **结果**：公开面 `yields` 与 `demands` 双双归零（原 6 与 79），层外连接获取归零（原 81 处 / 25 文件），`core-boundary-ledger.json` 的 rusqlite 引用归零（原 9）。**DD-147 冻结的残量已清零**：daemon 22 引用 / 19 SQL 与 scheduler 17 / 16 均为 0，`crates/daemon` 的 manifest 任何 section 都不再出现驱动（`forbidden` → `none`），`core` 的角色由 `persistence` 更正为 `forbidden`（它在 FR-130 Phase A 就不再是持久化层，而台账此后又宣称了四个 FR），驱动声明移入 `[dev-dependencies]`。DD-147 写下却从未实现的那条规则——"残量归零后声明本身开始失败"——由 `stale_residual_errors` 补上。`schema-snapshot.sql` 全程逐字节未变（语句逐条搬家，未改写）。搬不走的测试消费者（core 自己的测试用 `TestState`/`create_task_impl` 构造夹具，在层*之上*；以及按 Rust 设计就在 crate 隐私边界之外的 `tests/round_trip.rs`）经 `test-support` feature 取连接，而门禁**清点该模块而非跳过它**——跳过等于认证一项自己观察不到的豁免；生产代码调用它是 `E0433` 编译错误，已实测。
- FR-140 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/153-governance-execution-cost.md`、`docs/qa/orchestrator/191-governance-execution-cost.md`、`config/governance/ci-step-cost.json` 与 `scripts/qa/{ci-cost.rb,test-ci-cost.sh}` 承载。它是 FR-127 以来第一条针对治理**成本**而非治理**正确性**的立项，动机成立且经实测加强：run `30189806915` 的五个时长逐一吻合（治理 67 分钟 vs 产品自身测试 5 分钟），而到治理时该比例已升至 **80 分钟**（`governance` 3286s + `ci-environment-parity` 1512s），门禁数 62 / 29 也已漂到 **65 / 32**——本 FR"每个 FR 都在往上加而无人计数"的论点，由它自己数字的过期所证实。
  **但它把成本归错了地方，而这处更正重写了需求 2 的全部内容。** FR 称主要开销是隔离机制的实现——逐 case 全树复制。实测：`test-persistence-dependency.sh` 全部 22 次 `new_case` 复制合计 **6.2 秒**，而该脚本整体 195 秒，复制占 **3.2%**；真正的代价是**每棵隔离树上都要跑一次完整门禁**，而四道门禁各有 9–22 秒耗在同一个函数里——`RustLexer.mask_literals` 用 `String#[]` 逐字符走过 6.4 MB 的 Rust 并对每个字符调用一次 `raw_string_start`（415 文件合计 **37.4 秒**，而拿到掩码后的 `strip_test_modules` 只要 0.099 秒）。在最大的单文件上：现行实现 2726ms，什么都不做的 `String#[]` 空走 892ms，改走 `chars` 数组的同一趟空走 15ms——贵的不是遮蔽，是**走路**。另有一项边界：把治理 job 的每个门禁步骤按是否声明 `cargo` 分类并接上真实步骤时长，声明 cargo 的占 **1449s（63.6%）**，而"不优化 cargo 编译时间"是本 FR 自己的非目标，故按原文实施的需求 2 命中的是它获准触碰的那 36% 里的 3%。
  改法因此换成重写 `mask_literals`（走字符数组），它比原方案**更严格**地满足需求 2 自己的约束："以现有 fixture 套件的通过与否作为改造正确性的判据"——换 worktree 或按需复制会改变每棵 fixture 树装的东西，一条在新树上仍然通过的断言可能是因为新的理由；重写词法器则输入输出逐字节相同，不动任何 fixture、不动任何断言、不共享任何树，判据可以比"套件全绿"更强。实测：**415/415 文件逐字节相同**、26 条手写对抗构造全同、7000 条随机输入零差异；速度 37416ms → **1498ms（25×）**。逐套件（断言数均未减少）：`test-persistence-dependency.sh` 195s → 29s（22 条）、`test-core-boundary.sh` 360s → 57s（14 条）；对照组 `test-doc-lifecycle.sh` 与 `bash32-compat.rb` 均不是该函数的消费者，**均未移动**——若什么都变快了，那是换了尺子而非修好了缺陷。
  需求 3 的上限定为 **2700 秒（45 分钟）**，理由不取自当前值（FR 明令禁止）：45 分钟是本 FR 立项时**整条流水线**端到端的区间上界，这条线说的是"治理自己不得花掉整条流水线过去所花的时间"；定线时该二 job 实测 4798 秒，故它立刻且大幅地约束。复核条件写在台账里：新门禁塞不下时，加它的人要么腾出空间、要么在文件里书面抬高上限。另引入 `pendingMeasurement`——新加的步骤从未跑过、不可能有数字，该窗口须具名并写明理由，且**其间预算不予执行**（对一份已知缺步的总数比对上限，会报出不存在的余量），刷新时自动清除。
  附注中"`test-governance-ledger-tooling.sh` 在 CI 中从未成功过"已不成立（最近五次运行均 success）；`boundary-coverage` 连红 6 次属实但是历史事实，`ci-liveness.rb` 的文件头注释正以它作为该台账存在的理由。**本 FR 自身撞出了 FR-144**：往清单里把 `providerIsolation` 写成字符串而非对象，`jq` 中途报错退出 5，而 `done < <(jq ...)` 不观察退出码，`check_provider_isolation` 读到零行返回成功——**真实门禁报告 PASS，只有 fixture 套件的三条负向 fixture 失败**。
- FR-147 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/160-enforcement-manifest-completeness.md`、`docs/qa/orchestrator/198-enforcement-manifest-completeness.md`、`scripts/qa/test-qa-gate-surface.sh` 的 `check_workflow_execution_declared` 与 `scripts/lib/workflow_model.rb` 的 `executed_scripts` / `development_triggered?` 承载。缺陷属实且要紧：FR-143 之后仓库把扫描器的**范围**从 manifest 派生而不再手写清单，理由是"手写清单只守住写下它那天已知的东西"——但那只是把枚举搬进了 manifest，而没有任何东西拿 manifest 与"CI 究竟跑了什么"对账。三道 shell 门禁由 `ci.yml` 执行却不在清单里（`qa-doc-lint.sh`、`coverage-governance.sh`、`check-async-lock-governance.sh`），于是 `jq-status-observed.rb` 与 `fixture-target-drift.rb` **从未读过它们任何一个**；其中 `test-agent-driver-documentation-alignment.sh` 的 `invokedBy` 正是 `qa-doc-lint.sh`——**被调用方被治理，调用方不被治理**。两道既有 check 都看不见它：check 1 的磁盘集合来自 `find scripts/qa`，三者都在 `scripts/` 下；check 3 问的是反方向（已声明的条目是否真被执行）。**原文两条事实未通过核验，其中一条改变了做法**。（1）正向差集不是三项而是**六项**：FR §1 声明的方法是"每个 workflow job"，报的却是 `ci.yml` 子集，另三项（`package-release.sh`、`package-skills.sh`、`update-homebrew-formula.sh`）由 `release.yml` 执行、同样缺席。两条独立推导一致（`workflow_model.rb` 遍历 `WorkflowModel.workflows` 与 `.github/workflows/*.yml` 上的裸 `grep -ohE`，都给 36 个被执行的 `.sh` / 6 项未声明；扩到 `.rb` 为 42 个被执行、同样 6 项），这一致本身还顺带确证了**本仓库没有任何脚本只出现在注释或 heredoc 里**——若有，两条推导必然分歧。因此护栏取"全部 workflow"口径：因为今天的缺口都在 `ci.yml` 就把护栏收窄到 `ci.yml`，正是 §4.4 shape 2 施加到护栏自身，且四个 workflow 里已有两个在跑治理门禁。（2）FR §2 说"补条目会同时点亮两道无关门禁"，并以此作为 FR-145 把这件事排除在外的理由——**在提交任何东西之前实测为假**：六个 manifest 消费方全绿，两个扫描集各自恰好增长三个（36 → 39、31 → 34），两道 meta 门禁各 18/18。验收标准第 4 条因此在编辑的那一刻就已满足，而不是事后论证。反向差集是七项、全部带 `invokedBy`——此条属实，且已由 `check_wiring_truth` 治理，故新 check 只管正向。**第三条是 FR 没有的发现，且是本 FR 唯一可能引入假保证的地方**：`coverage-governance.sh` 无法诚实地声明 `no-provider`。`check_provider_isolation` 的 `no-provider` 分支是在**门禁自己的 shell 文本**里 grep `fixtures/**.yaml`，而这道门禁跑的是 `cargo llvm-cov --workspace --all-targets --all-features` 加 npm 与 Playwright 套件，可能触及的 bundle 写在 Rust 与 TS 里，循环读到零行然后通过。二阶后果更糟：声明 `no-provider` 会把门禁判为"非 provider-capable"，从而**抑制** `boundary-coverage` 上的 `check_provider_stub_coverage`——而该 job 当时并不装 stub，也就是说这条声明会把唯一能反驳它的兜底一并移走。处置：给 `boundary-coverage` 与 `coverage-policy-fixtures` 都装上 `./.github/actions/provider-stubs`（遇真 CLI 退 97 并大声失败），把无法核验的声明换成被执行的兜底；残余限度记入 DD-160 已知限制而非当作已解决——stub 让声明在运行时可观察，并没有让它静态可核验，也还没有任何东西强制未来"编译并跑测试"类的 `no-provider` 门禁装 stub。豁免侧：三个发布脚本以 `supportFiles` 的第三个角色 `release-tooling` 声明，三条约束防止它变成 §4.4 shape 8 的那种毯子——**逐路径**书写不写目录或通配；**条件挂在触发器上**，任何执行它的 workflow 一旦跑在分支 push 或 PR 上即失败，且该判断由每个 workflow 解析出的触发器映射派生而非靠 workflow 名单；以及**被直接执行的脚本若声明成其他角色即失败**，因为 `fixture` 与 `library` 的定义都是"从不作为门禁本身被调用"，否则改一个词（`release-tooling` → `library`）就能既保留声明又逃出触发器规则——这正是 fixture 26 施加的变异，比 fixture 25 的删除更便宜。**两处缺陷由仓库自己的门禁在本 FR 施工中查出，均属实**：`fixture-target-drift.rb` 判定新增三个 fixture 是"未证明落地的原地改写"，并指出 fixture 27 里的 `raise` 是 abort 而非失败断言（若锚点移位，`set -e` 会带走整个 run 且汇总行不打印）——改为依赖 `fixture_mutate` 的摘要比对，锚点消失时不写文件、由它报一条点名的失败断言。同时暴露该门禁**看不透本地别名**：它按语句首词识别落地证明，而 `test-qa-gate-surface.sh` 走 `inject()`；约三十个旧调用点之所以从未被报，是因为它们用 `perl -pi -e` 改写，不匹配其原地改写模式，且 `WRAPPERS` 只认 `if <fn>` 不认 `elif <fn>`——已记入 DD-160 已知限制。另一处：fixture `BASE` 的 tar 清单本身就是过期枚举，本 FR 撞上了它——它按名字拷 `scripts/qa-doc-lint.sh` 却没有另外两道 `scripts/` 下的门禁，新 check 会在**每一个** case 里把它们报成未声明，读起来像"check 有效"，实际是每个 fixture 都因 fixture 自己的原因失败而非它声称测试的原因；现改为从 manifest 派生，派生不出东西就判红。另修正六处已被本改动作废的陈述：manifest 自己的 `description`、CHANGELOG 的 FR-127 与 FR-134 两条、`docs/qa/orchestrator/177` 的 Scope、以及 DD-145 与 DD-158 各一段（后两者的结论都成立，作废的是它们据以论证的前提"清单的声明范围是每个 `scripts/qa` 门禁"；DD-158 那段尤其值得留痕——它当初正是用这个前提解释为什么一个 `cargo test` 模块不必进清单，而新规则改以"job 是否执行该脚本"表述后，结论反而站得更稳）。**认证记录（`099b6169`，前后同一 revision，worktree 前后皆干净）**：门禁集合由 manifest 派生而非手写——`jq '.scripts[]|select(.enforcement=="ci-required").path'` 得 **46 条**（本 FR 前为 43），再用 `workflow_model.rb run-commands` 逐 workflow/job 取真实调用行反查，得 51 行含 `scripts/**` 的调用，其中引用 ci-required 路径的 **46 个调用**（含 `--fixture-test`、`--tool-fixtures` 等变体，`FR126_FAST=1` 前缀，以及 `certify-slack-managed-live.sh status` 这个裸跑会退 2 的子命令）；46 条里 39 条直接被调用、7 条经其声明的 `invokedBy` 包装器覆盖，**未覆盖集合为空**。每条以 `bash -c "$cmd" > log 2>&1` 直接取 `$?`，不经管道。**46/46 跑完，2 条红，均非本 FR 回归**：`ci-liveness.rb`（台账停在 `2b2e5cab`，而 ci.yml 此后已变——**以 `9fa37e37`（本 FR 之前）的 before-run 确证同样为红、同样的诊断**，故不是本 FR 引入）与 `test-dependency-policy.sh --tool-fixtures`（本机无 `cargo-deny`，日志诊断为 `error: no such command: deny`，该变体由 `security.yml` 执行，与 revision 无关）。46 份日志逐份确认非空且末行完整（无提前终止），其中 `check-async-lock-governance.sh` 的判决行「Async lock governance check passed.」在首行、`coverage-governance.sh` 的「coverage governance passed」在产物清单之前——两者末行是路径而非汇总行，已逐一核对而非按末行形状推断。本 FR 新分类的四个调用全绿，含 `coverage-governance.sh` 裸跑的完整 `cargo llvm-cov` + npm + Playwright 一轮。另：`test-qa-gate-surface.sh` 主模式 14/14，`--fixture-test` **37 passed / 0 failed** 且汇总行在，fixture 25/26/27 各自只挂 `check_workflow_execution_declared`（`expect_fail` 会在同一棵树上跑其余每一道 check 来确证隔离）；`cargo test --workspace --exclude orchestrator-gui` 2725 passed / 0 failed / 2 ignored，strict clippy 干净（首次 cargo 复核因在 zsh 下用了 `${PIPESTATUS[0]}`（zsh 数组从 1 起）且把 cargo 管进了 `tail`，**根本没取到退出码**，已按 §4.6.4 重跑并直接取 `$?`）。**一件值得记的事**：`qa-doc-lint.sh` 一旦被纳入清单，立刻查出本 FR 自己新写的 QA 198 三处缺陷——6 个场景超出 5 的上限、`docs/qa/README.md` 索引缺行、缺 `## Checklist` 段。这三处与 manifest 无关，本来就会挂 CI；但它同时说明第一次 sweep 期间我在改文档，那一轮按 §4.6.2 作废并在干净树上从头重跑，此处记录的是第二轮。
- FR-149 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/159-dd137-fixture-residue-retirement.md` 与 `docs/qa/orchestrator/197-dd137-fixture-residue-retirement.md` 承载。FR-148 冻结的 19 条 `rotted` 全部删除，`rotted_count` 归零——等值棘轮正是逼着账本条目与文件同一个 commit 出去的机制。**原文三条事实未通过核验**：（1）C 档不是 14 个孤儿而是 12 个，`qa107-s1-parallel` 与 `stagger-test-scenario3` 是 QA 196 负向 fixture 的**点名目标**，B 档的 `cycle-overflow-test` 也被其预期结果点名——衡量这堆残留的那道门禁，其 fixture 正指着清理要删的文件，账本 `consumers` 字段同源漏了；（2）需求 4 的未决问题已答：删 19 个 bundle 令 **22 个 workflow ID** 离开 qa-doc-lint 的派生集合（两条独立推导一致，158 → 136，0 处 parse 失败），与 QA 文档 `--workflow` 引用相撞的**只有一个**（`narrow-test`，且就在同一 commit 被 supersede 的那份文档里），风险全在 B 档而非 FR 猜测的 A+B；（3）DD-158 的「`behavior.captures` 9 个」两条推导都不给 9（按诊断 8、按文件内容 10），且其枚举求和 32 与自称的 31 差一。**第四条在实现中才倒**：FR-148 记 `test-wp05-integration.sh` 「与另一道同病同 commit」，两点都错——把它**跑**一遍，它死在 `ensure_db` 的 `orchestrator init`（`daemon socket not found`）、**走不到 L1-A**，病因是 `1be4666d`（2026-03-26 CLI/守护进程拆分），早四个月；那三个 rotted bundle 确实在那三行、确实会失败，正是这一点让错误病因听起来足够合理。教训与本 FR 主题同源：**读 fixture 不等于跑产品，而那句判断是靠读门禁得出的**。处置：qa-doc-lint 的 workflow-ID 检查限定在 `lifecycle: active` 文档，豁免集合由 frontmatter 派生（不用已提交索引，避免依赖另一道门禁的新鲜度）、**派生不出来就查全部并判红**、且每次运行打印豁免了谁；`scripts/qa/test-qa-doc-lint-workflow-scope.sh`（ci-required）逐条守住这三性质，其中 case 6 破坏**第三份**文档的 frontmatter，证明豁免会随作用域失效而蒸发而不是被缓存——两个变异实测：让派生失败时豁免一切只挂 case 6，去掉 lifecycle 跳过只挂 case 2/4/5。QA 196 的三个负向 fixture 已重新武装（目标与期望值一律从账本派生，各带一次 before-run）；WP05 门禁 harness 重写（隔离 `ORCHESTRATORD_DATA_DIR`、脚本自起自收守护进程、按包构建两个二进制），**自 2026-03-26 以来第一次跑绿**：SELECTED 2 / PASS 8 / FAIL 0，summary 行在。**认证记录（`de0f8b8e`，前后同一 revision，worktree 前后皆干净）**：门禁集合由 manifest 派生而非手写——`jq '.scripts[]|select(.enforcement=="ci-required").path'` 得 43 条，再用 `workflow_model.rb run-commands` 逐 workflow/job 取真实调用行反查，得 **46 个调用**（含 `--fixture-test` 等变体，以及 `certify-slack-managed-live.sh status` 这个裸跑会退 2 的子命令）；43 条里 36 条直接被调用、7 条经其声明的 `invokedBy` 包装器覆盖，差集为空。每条以 `bash <inv> > log 2>&1` 直接取 `$?`，不经管道。**46/46 跑完，2 条红，均非本 FR 回归**：`ci-liveness.rb`（本 FR 动了 ci.yml，台账记录停在 `2b2e5cab`；`--refresh` 读 `gh run` on main，只能推送后刷新——已核实本 FR 之前它是绿的）与 `test-dependency-policy.sh --tool-fixtures`（本机无 `cargo-deny`，该变体由 `security.yml` 执行，与 revision 无关）。另外 `cargo test --workspace --exclude orchestrator-gui` 2725 passed / 0 failed，strict Clippy 干净，`test-wp05-integration.sh` SELECTED 2 / PASS 8 / FAIL 0 且 summary 行在。46 份日志逐份确认末行完整（无提前终止）。
- FR-148 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/158-fixture-bundle-validity.md`、`docs/qa/orchestrator/196-fixture-bundle-validity.md`、`core/src/fixture_corpus_tests.rs` 与 `config/governance/fixture-bundle-validity.json` 承载。缺陷属实：`test-coordination-collapse.sh` 自 07-25 起跑不完，而这一点不需要门禁跑起来就能发现——fixture 写着 `behavior.captures`，校验器拒绝 `behavior.captures`，只是没有任何东西把两者对上。但**规模被低估了一个数量级，且两条"故意无效"的判断是反的**。（1）**31 / 93 个 bundle 当前被拒，不是 4 个**；其中 **19 个**是 DD-137（`1b0937ca`，07-25）退役的构造：`[legacy_coordination_removed]` 8 个、`[legacy_json_path_removed]` 10 个、prehook schema 漂移 1 个（此处原写作「`behavior.captures` 9 个」，两条推导都不给 9——按诊断是 8，按文件内容含 `captures:` 的是 10，因为两个 wp05 bundle 两种构造都带；且原枚举求和为 32 而非其自称的 31，`prehook-test` 在 19 之内不在其外。由 FR-149 核出并改正）。（2）**坏掉的门禁不止一道**：`scripts/qa/test-wp05-integration.sh:250,282,312` 对三个被拒 bundle 执行整份 `apply`，同样是 `manual-runbook`，同样没人看见——**一例是缺陷，两例是一类**，这正是要机制而不是要第二次修补的理由。（此处原写作「与触发本 FR 的那道是同一个病、同一个 commit」，**两点都错**：FR-149 把这道门禁**跑**了一遍，它死在 `ensure_db` 的 `orchestrator init`——`daemon socket not found`——**根本走不到 L1-A**，更够不着那三个 bundle；病因是 `1be4666d`（2026-03-26，CLI/守护进程拆分），比记录的早四个月。那三个 rotted bundle 确实在那三行、确实会失败，正是这一点让「同一个病」听起来足够合理，以至于没人真的跑一遍去核。此处的教训与本 FR 的主题同源：**读 fixture 不等于拿产品跑它，而上面这句判断正是靠读一个门禁得出的**。）（3）`qa105-s1-capture-wrong-level.yaml` **通过校验**：step 级的 `capture:` 是未知键，被静默忽略，产品根本不拒绝它想谴责的那个写法；`crd-test-invalid.yaml` 确实被拒，但命中的是 `no CustomResourceDefinition found for kind 'PromptLibrary'`，它设计上要演示的「缺 `prompts` 必填字段」要先 apply `crd-test.yaml` 才够得着。（4）原文漏掉了**第五个**故意无效的 bundle，而它是四个里唯一有活消费者主张这一点的：`coordination-strangler-parity.yaml`，由 **ci-required** 的 `test-coordination-strangler.sh:126-133` 断言其被拒且诊断为 `[legacy_*_removed]`。（5）**校验结果依赖 base，因此 base 与账本一起写明**：`validate_manifests` 先把 bundle 并进当前 config 再 build，所以"产品是否接受"从来不是文件单独的属性——用 `TestState` 原样的种子配置时 60/33，其中 **5 条假失败**是 bundle 引入 self-referential workspace 后把**脚手架自己的 `basic` workflow** 拖下水（`[SELF_REF_POLICY_VIOLATION] ... workflow 'basic'`），与 fixture 无关；清空 agents+workflows 后 62/31，那 5 条消失，5 个片段 bundle 转红而那是关于它们的真话。机制走 `service::system::validate_manifests`（守护进程收到 `ManifestValidate` 时调的同一个），跑在既有 `Rust test` job 内，93 个 bundle 共用一个 `InnerState` 合计 **1.77s**（每个新建一个是 24.34s），新增治理步骤 0、预算占用 0。**断言的是诊断而不是退出码**：能力校验发生在退役检查之前，缺 agent 的清单以 `no agent supports capability` 失败，光看退出码分不开它与 fixture 要演示的退役——负向 fixture 4「被拒、已声明、但理由变了」正是退出码检查造不出来的那一条。`expect` 是列表，因为一份带多个退役构造的 bundle 由合并顺序决定先撞上哪个，而合并走 `HashMap`（`cycle-overflow-test.yaml` 连续两次跑出不同的 workflow 名）；每条按 workflow 逐一列出，列表是枚举而非通配，空 `expect` 或含空串的 `expect` 本身即违规——空串匹配一切，会把该条变成对未来任何拒绝的概括接受。语料范围由 `git ls-files` 派生，`git` 跑不起来、通配匹配不到、账本解析不了**都判失败而非跳过**；注入 fixture 同理不点名目标文件，改用「第一个被接受且未声明的 bundle」在运行时派生。19 条 `rotted` 以**等值**而非上限冻结，清理由 FR-149 承接（含退役 `test-wp05-integration.sh` 与翻转 QA 51 / 83 / 84 / 92 四份至今 `lifecycle: active` 却在描述已移除机制的文档）。**闭环 sweep 还撞出一处共享工具的范围谓词问题**：新模块最初叫 `core/src/fixture_corpus.rs`，它文档注释里 3 行加 YAML 常量里 1 行提到 `behavior.captures`，直接把 coordination 台账的 `capturesOrJsonPath` 从 53 顶到 57，令 **ci-required** 的 `test-coordination-strangler.sh` 变红。原因是 `scripts/lib/rust_source.rb` 排除测试源码靠的是**文件名**（路径含 `tests` 组件，或 basename 匹配 `/test.*\.rs\z/`），**不是 `cfg`**；工作区里已有的 14 个文件级 `#[cfg(test)] mod x;` 模块碰巧全都叫 `*test*.rs` 才被排掉。改名为 `fixture_corpus_tests.rs` 而不是抬高基线——抬基线会用测试代码稀释一条主题恰恰是生产消费者的棘轮。**陷阱仍在**：下一个不带 "test" 字样的纯测试模块会静默地把四条台账一起顶高，这是 §4.4「范围谓词也是断言」发生在共享工具里，已记入 DD-158 已知限制。已知不覆盖：门禁 shell 本体的断言腐烂（修票时找到的第二条即是——`normalize_preserved_channels` 把四个通道搬进类型化载体而断言还在查旧形状），本 FR 原文即已写明，不假装覆盖
- FR-146 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/157-pipefail-short-circuit.md` 的「第二个家族：`head`」一节与 `docs/qa/orchestrator/195-pipefail-short-circuit.md` 的 case 18–22 承载（与 FR-145 合用一对文档：同一个 EPIPE，不同的调用位置，拆成两份设计记录只会把一个故事劈成两半）。机制成立，但**五项事实陈述未通过复核**。最要紧的一项：后果表是**部分反的**，而且漏掉了占比最大的那一类——原文预测条件位置会「断言反转，同 FR-145」，实测条件位置**存活且取值正确**（`set -e` 在条件里是关的，`[ -n … ]` 消费的是值）；而原文的表里**根本没有「赋值」这一行**，`X="$(p | head -1)"` 正是 37 处里的约 18 处（`task create … | grep -oE UUID | head -1`），它们不反转任何断言，它们让门禁在走到汇总行之前**直接停掉**。第二项：`head` 比 `grep -q` **猛两个数量级**——`X="$(seq 1 N | head -1)"` 每档 10 次，3.9 KB 时 0/10，**24 KB 时 6/10，129 KB 以上 10/10**，而 FR-145 的 `grep -q` 在 90 KB 上是 8–13 / **400**；而且 `head` **不需要任何 flag**，`grep`/`rg` 只在被告知时才短路。因此修法也不是 here-string：`sed -n '1,Np'`、`awk 'NR<=N'` 与 `${out%%$'\n'*}` 都读到 EOF 或根本不用管道（1.3 MB 实测）。其余三项：38 处实为 **37 处**（两处是单引号字符串里的正则或选支 `(get|post|put|delete|patch|head|options)`，且原文的范围口径已被 FR-145 自己的闭环作废——放宽范围 +1、走词法器 −2，**两个独立错误几乎互相抵消**）；需求 1 的逐处分类是文档而非决策输入（修法在任何位置都一样）；`|| true` 不只是被拒绝的修法，它是既有状态，**4 处随被保护的读取方一起消失**——它们在吸收 head 的 SIGPIPE，同时也在吸收真实的 `diff` / `jq` 失败，是 FR-144 那一类被本缺陷的绕过方案续命的盲点。护栏的 fixture 从 19 增至 24，bash 3.2.57 与 5.3.9 双绿，其中两条是让规则可用的关键：**三种修法都不得触发**（否则每个人修完都会再点亮它），以及 **`head -5 file` 作为第一段不得触发**（它上游没有产出方可杀，禁掉唯一明确安全的写法比没有规则更糟）。另外发现两道 manual 门禁早已烂掉，均以 before-run 确证与本 FR 无关，各记一张 ticket：`test-control-plane-action-audit.sh`（`schema_migrations.version` 唯一约束冲突，退出 19 且**不打印汇总行**）与 `test-non-code-workspace.sh`（attention handoff 未投影）
- FR-145 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/157-pipefail-short-circuit.md`、`docs/qa/orchestrator/195-pipefail-short-circuit.md`、`scripts/qa/pipefail-short-circuit.rb` 与 `scripts/qa/test-pipefail-short-circuit.sh` 承载。机制属实并可复现，但**七项事实陈述未通过复核，其中两项改变了做法**。第一项也是本次治理最重要的一条：**这条缺陷的方向不是固定的，由命中路径通向哪个分支决定**。FR 把它登记为失败方向"闭"（产出莫名其妙的红，人重跑到绿），而那只对命中⇒通过的站点成立；命中⇒失败的站点上，同一份代码把**真实违规报成干净通过**，实测 2 / 200。仓库里这样的站点有五处，其中三处是泄漏断言，全部形如 `! sqlite3 "$DB" .dump | grep -q SECRET`——整库 dump 是全仓唯一无界的产出方，另两处是 `cargo test --workspace 2>&1 | grep -q "^test result: FAILED"`，一个失败的测试套件可以报成 `PASS: cargo test`。**立 FR 的人没有看见这一向，因为观察到的那一个实例恰好是无害的那一半**。第二项：FR 要求的「逐处写明产出方为何有界」不可判定——90 KB 产出方（命中在第 59273 字节）触发 8–13 / 400，1 MB 单行产出方（命中在第 0 字节）触发 0 / 200，决定因素是命中位置与行结构而非尺寸，且 `rg` 与 BSD `grep` 不同；何况「有界」是关于**今天的数据**的断言，没有任何东西复查它。故改为语法规则且**不设豁免口**：here-string 形式永远可用且永远正确。其余五项：空闲触发率不是 0 / 400 而是 8–13 / 400（负载不是前提，FR 由此推出的"为什么从未被看见"是错的）；「42 处 / 9 道门禁」实为 **35 处可执行站点 / 7 道门禁**，42 由 `grep -c` 数出而它数的是行，其中 4 行是描述该形状的注释——**一份专门讲断言强度的 FR 犯了 §4.4 shape 1**；十二个"可疑"候选里有十个因 `scan() { (...) || true; }` 结构上免疫；报错的不是 `qa-doc-lint.sh` 而是它 `invokedBy` 的那道门禁。覆盖面是**全部 tracked `.sh`，不设任何豁免**——包括不开 pipefail 的文件。这一点是闭环自检时改的：原先豁免它们，理由是「没开 pipefail 就没有危害」，而 **shell 选项是动态的**：`scripts/regression/run-cli-probes.sh` 自己 `set -euo pipefail` 然后 `source` 每一个 `scenarios/` 下的文件，那两个 FR 记作「免疫」的文件因此一直是活的站点，并且被门禁的第一版漏掉。已用执行而非论证证明：一个被 source 进 pipefail shell 的 scenario 会把**确实存在**的模式报成不存在。合计 **63 处 / 22 文件**。**复核还查出这个形状三天前就被找到过一次**：FR-134 于 2026-07-26 在 CHANGELOG 的 `[Unreleased]` 写下 "Every such pipeline in the governance scripts is now a here-string"，而 DD-145 写的是 "in these files"——一次局部修复被记成全局修复，那句话在写下的那一刻就是假的（当时还剩 61 处），三天后其中一处撞出了本 FR。该条目已在本次闭环中改正。两项已知限制另立后继：`| head` 是同一动作不同后果面（38 处 / 29 文件，其中 8 处诊断位置实测让脚本退出 141 并中途终止，汇总行不打印）→ FR-146；`qa-doc-lint.sh` 与 `coverage-governance.sh` 由 `ci.yml` 执行却不在 `qa-gate-surface.json` 里，每一道派生扫描器都看不见它们 → FR-147。另发现一道 manual-runbook 门禁 `test-coordination-collapse.sh` 早已中途终止（守护进程拒绝其 fixture 使用的 `behavior.captures`），以 `596500e7` 的 before-run 确证与本 FR 无关，记为 ticket
- FR-144 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/154-jq-status-observed.md`、`docs/qa/orchestrator/192-jq-status-observed.md`、`scripts/lib/gate_jq.sh` 与 `scripts/qa/{jq-status-observed.rb,test-jq-status-observed.sh}` 承载。缺陷属实并已完整复现：在 scratch clone 上把一处 `providerIsolation` 由对象改写为字符串，`jq` 以 5 退出，真实门禁 `test-qa-gate-surface.sh` 输出 `13 passed, 0 failed`（rc=0），而它自己的 fixture 套件同树输出 `6 failed, 28 passed`（rc=1）——**一道门禁与它自己的负向 fixture 给出相反结论时，对的那个是 fixture**。三处未通过复核：（1）**范围低估 2.3 倍**——原文数的是文本 `done < <(jq` 共 17 处，而缺陷是「供给端能否走到 jq」这一可达性属性，做传递闭包后为 **39 处**，且排序颠倒：原文把 `test-qa-gate-surface.sh`（13 处）当作重灾区，实际最严重的是被列成只有一处的 `test-docs-publishing-integrity.sh`（**22 处**）；（2）**存在原文完全未提的第二条通道**——每个 check 都以 `"$check" "$root" || return 1` 即条件位置调用，条件位置对整棵调用树关闭 `set -e`，故普通命令替换同样静默（已实测：同一函数条件位置调用 rc=0 且结果为空，裸调用则以 5 退出）；（3）失败 fixture 是 **6 条**（5、6、10、11、12、20）而非原文的 3 条，且其中 20 属于 `check_provider_stub_coverage`——**两道 check 读同一字段**，因此本 FR 的类型错误 fixture 无法套用 FR-127 的「隔离到单一 check」约定，已改为断言整道门禁失败并在文档中写明理由。
- 「零行」必须由调用方声明（`require-rows` / `allow-empty`，**无默认值**）的理由由方向给出而非风格：同一份清单里 `check_surface_complete` 读空会让磁盘上每个文件显得未分类，**向失败一侧倒**；`check_provider_isolation` 读空则让循环体一次不执行，**向通过一侧倒**——而代码里没有任何东西区分这两者。逐调用点捕获**并不充分**：`test-docs-publishing-integrity.sh` 在四层嵌套进程替换内部读策略，非零返回无处可去，故失败另行落盘（子 shell 不能回传状态但可以写文件），门禁在末尾问一次。实测注入 `.sources` 类型错误时 `check_policy_fresh` **对一份它读不了的策略报告 PASS**，仅靠该运行级记录才转红——这一条单独证明了该机制值得存在。
- 本 FR 由撞上缺陷的人当场写下、未经复核，因此它自己犯了它所记录的那一类错误：用一个廉价的文本代理代替了要测的事实。三处实现缺陷是**跑出来的而不是读出来的**：`gate_jq_rows` 最初以 `rows="$(jq …)"; status=$?` 读状态，这只在 `set -e` 已被抑制处成立，在 `set -e` 生效的进程替换内该赋值直接触发 ERR，shell 在 `status` 被读取前就离开，失败记录从未写下——**FR 自身的缺陷在它的修复内部重现**；扫描器的可达性表把「问过且答否」当作「可达」，误报了一个 awk 函数与一个 ruby 函数；fixture 脚本自身的 pass/fail 文案在双引号内引用了被禁模式（词法器按设计将双引号区域视作代码），并使用了 governance job 未提供的 `python3`。
- FR-130 已闭环删除；其三个阶段的设计与验证现由 `docs/design_doc/orchestrator/{142-core-boundary-freeze,148-persistence-crate-extraction}.md`、`docs/qa/orchestrator/{180-core-boundary-freeze,186-persistence-crate-extraction}.md`、`config/governance/{core-boundary-ledger.json,schema-snapshot.sql}`、`crates/orchestrator-persistence/` 与 `scripts/qa/{core-boundary.rb,test-core-boundary.sh,test-persistence-extraction.sh}` 承载。`core` 从 **200 处 `rusqlite` 引用 / 37 文件降至 9 处 / 3 文件**，`52 / 924 / 143`（`pub mod` / 公开项 / 文件）降至 `50 / 658 / 129`，全程 18 个搬迁提交各自具名可回退，`config/governance/schema-snapshot.sql` **逐字节未变**。

  闭环判据不是「三个 phase 都做完」也不是「还有残余所以不能关」：**「保留并记录理由」与「被阻塞」是合法的闭环状态，因为各自带着一个决策和一个后继；没有决策的不是。** 18 个 Phase B 文件各有书面结论（15 迁出或拆分、3 保留并记录理由），残余 3 处文件全部指向 FR-141——而 FR-141 的非目标写明须在 Phase B 闭环之后开始，所以本 FR 闭环恰是那个后继的前置条件。Phase C 亦非原文预设的二选一：实测 `impl From<rusqlite::Error> for OrchestratorError` 只有一个消费者，搬走那段 SQL 后它即为死代码并被删除，其保证的 `ExternalDependency` category 由具名映射函数显式承接并经反向变异钉住。

  三件交给后继而非随本 FR 消失的事：`persistence/repository/config.rs` 原记作「被阻塞于 crd 下沉」，收尾时重新推导发现**那个解锁条件回答的是 Phase A 的问题**（整个文件能否下沉——会成环）而非 Phase B 的（语句能否下沉——不需要任何 crd 类型），故无需为 crd 立 FR；**保留的三个文件的 SQL 护栏从未被审计**，而做审计的机制（搬迁时不得不逐条读语句）永远照不到它们，已随调用点一并移交 FR-141；触发历史上限**从未生效过**（`task_items` 引用 `tasks(id)` 无 `ON DELETE CASCADE`，任何真跑过的 task 都删不掉，调用方只记日志），已记入 DD-148 的 Known limits，其修法需先回答一个产品决策而非重构问题。

  搬迁本身产出的最大价值不在计数：五条 routing 安全护栏此前无任何测试覆盖、一个 resume 竞态被顺手关掉、多条断言在第一次变异下就通过并各自暴露断言自身的缺口。DD-148 记下两条变异测试的系统性失效方式——一条不变量可能由多条语句各持拷贝；施加于语句的断言不等于施加于守卫的断言。
- FR-139 已闭环删除；其三条修正（删除 `classification_errors` 中比较同一归约、任何输入都无法使其失败的总和分支，并同步改写 DD-147 与 QA-185 中把它当作现行保证的陈述；`SQL_STATEMENT` 补入 `PRAGMA` 并让引号锚跨过前导转义序列，台账 112 → 114 且增量恰为 `orchestrator-security/src/lib.rs` 与 `slack-gateway/src/store.rs` 各 +1；扫描面并入各 member 的 Cargo build script，台账新增 `scanRoots` 冻结实际读取的根）现由 `docs/design_doc/orchestrator/147-persistence-dependency-chokepoint.md` 的 Corrections 与 Known Limits 两节、`docs/qa/orchestrator/185-persistence-dependency-chokepoint.md`、`config/governance/persistence-dependency-ledger.json` 与 `scripts/qa/{persistence-dependency.rb,test-persistence-dependency.sh}`（12 → 18 条断言）承载。DD-147 不作 superseded：它仍是该机制的现行记录，本 FR 修正的是其中的陈述而非替换机制。

  三条中只有第二条改变了数字。`+2` 的双向判据是"修了缺陷而非换了口径"的证据本身——放宽匹配的诱人修法会把 `crates/cli/src/commands/guide.rs` 的 20 条帮助文案读成 SQL，故 case 14 以"日志散文必须**不**被计入"与 case 12 的"`PRAGMA` 必须被计入"同等强度断言，且两者在同一个文件上进行，使前者的绿不可能来自"这个文件根本没被读"。第三条选择放宽扫描面而非收窄散文，理由是条件 1 早已把 `[build-dependencies]` 归为生产声明，收窄会让两个条件对"生产"的定义不一致；五个 build script 当前均无驱动无 SQL，故台账 `references` 不因此变化。`scripts/lib/rust_source.rb` 因此接受单文件作为扫描根，`core-boundary.rb` 仍为 `200 / 37` 与 `52 / 924 / 143`、coordination 四条棘轮仍为 `53 / 30 / 9 / 0`，即该改动是纯增量的双向验证。

  未修而记录：`test*.rs` 的排除按**文件名**而非 `cfg(test)` 判定，`crates/orchestrator-runner/src/test_env.rs`（`lib.rs:23` 为无条件的 `pub(crate) mod test_env;`）是当前唯一活例，当前无驱动无 SQL。该规则与 `core-boundary.rb` 共用，改动会移动其已评审的 `200 / 37`，属另一次独立评审，已记入 DD-147 的 Known Limits。
- FR-130 的需求 2 Phase A（`orchestrator-persistence` 提取）已闭环，FR 文档保留并降为剩余的 Phase B/C；其设计与验证信息现由 `docs/design_doc/orchestrator/148-persistence-crate-extraction.md`、`docs/qa/orchestrator/186-persistence-crate-extraction.md` 与 `scripts/qa/test-persistence-extraction.sh` 承载。四次可独立回退的提交把 core 从 143 文件 / 52 `pub mod` / 924 公开项 / 200 处 `rusqlite` 引用（37 文件）降到 129 / 50 / 665 / 86（20 文件），`schema-snapshot.sql` 逐次字节不变，两个治理台账与代码同 commit 重新冻结。两处成员相对 FR 原表调整，均为结构性必然：`persistence/repository/config.rs` 留在 core（它读 `crd`，而 `crd` 回调 `db`，同时下沉即成环），`session_store.rs` 移入 Phase A（Phase A 的 `repository/session.rs` 依赖它）。115 处收敛 114 处，残余 1 处（`core/src/migration.rs` 的 `use rusqlite::Connection`，一个无人引用的兼容包装）已具名并归入 Phase B。**Phase C 亦已闭环**：`impl From<rusqlite::Error> for OrchestratorError` 已删除——原文的两个选项都预设了不存在的消费者，实测它只有一个（B1 搬走的那段 SQL），删掉后即为死代码；它保证的 `ExternalDependency` category 由调用点具名映射显式承接并有反向变异实测。**Phase B 亦已闭环**：18 / 18 文件各有一条书面处置（15 个已迁出或拆分、3 个保留并记录理由），core 降至 **9 处 / 3 文件**，残余全部是持久化层交出的驱动连接类型，归 FR-141；逐文件结论见 FR 内的处置表。其中 `persistence/repository/config.rs` 的处置在收尾时被更正——原记作「被阻塞，解锁条件是 crd 先下沉」，而该条件回答的是 Phase A 的问题（整个文件能否下沉），Phase B 问的是语句能否下沉，那些语句没有一条需要 crd 类型；所以不需要立 FR 承接 crd 下沉，也没有任何东西因 FR-130 闭环而失去归属。同时明确记下一件本机制照不到的事：Phase B 的护栏审计靠的就是搬迁，所以三个保留文件的 SQL 不变量从未被审计，一并交给 FR-141；同时留档：Phase B 引用中 61% 是驱动错误管道而非 SQL，因此存在「不搬一条 SQL 就收敛约 39 处」的捷径，已明确拒绝并写下理由。结构证据由既有两个台账门禁承担，新门禁只做它们看不见的部分：把 core 的依赖行**注释掉**后必须编译失败、中断点扫描与数据库记录的已应用行数对比、跨全部被搬动模块的真实写读回环（配未迁移库的负向用例）、以及 cargo 解析树中不得存在从该层回到 core 的路径
- FR-136 已闭环删除；其收口决策（以 `agent_orchestrator.db` 而非 crate 层级划线：core／未来的 `orchestrator-persistence` 为持久化层，`orchestrator-scheduler` 与 `daemon` 禁止并冻结残量至 FR-130 偿清、`task_state.rs` 在被禁止的一侧，`orchestrator-security` 因位于 core 之下而书面豁免、`slack-gateway` 因自有 `gateway.db` 而不在范围内、`integration-tests` 冻结于 `[dev-dependencies]`）、core 之外全部 55 处引用的机器可读分类，以及**两条互不替代的**门禁条件（谁可以*声明*驱动，由 `[workspace] members` 发现并逐 section 解析 manifest 得出；谁可以*使用*，按文件冻结 SQL 语句数与驱动引用数并双向精确比较），现由 `docs/design_doc/orchestrator/147-persistence-dependency-chokepoint.md`、`docs/qa/orchestrator/185-persistence-dependency-chokepoint.md`、`config/governance/persistence-dependency-ledger.json`、`scripts/qa/{persistence-dependency.rb,test-persistence-dependency.sh}` 与 `.github/workflows/ci.yml` 承载。`core-boundary-ledger.json` 的 `rusqliteDependentCrates` 随之删除，规则只在一处表达。

  FR 原文的事实偏差：非 core 的"23 个文件 75 处引用"来自对 `src/` 的朴素 `grep`，把测试代码一并计入，与它所引用的 DD-142 口径（`RustSource.scannable_source` 剥离 `#[cfg(test)]`）相反——按同一口径 core 精确复现为 200/37，非 core 实为 **15 个文件 55 处**（scheduler 17 而非 37、security 6 而非 7、slack-gateway 10 而非 9，daemon 22 正确）；被点名为生产消费者的 `service/task.rs`(4) **一处生产引用都没有**，4 处全在第 462 行 `#[cfg(test)]` 之下；判定性用例 `task_state.rs` 是 8 处而非 9 处；`spawn.rs`(3) 实为两个不同文件。

  但真正改变答案的是原文从未建立的三条结构性事实：`orchestrator-security` **位于 core 之下**（`core/Cargo.toml` 依赖它，反向不成立），它按路径自开连接正是因为不能向上依赖，原文却把它当作可迁移的同层消费者；`slack-gateway` **没有任何工作区依赖且拥有另一个数据库**（`config.rs:23` 明称 gateway-owned），把它接入共享持久化 crate 会制造当前并不存在的耦合；而 `rusqlite` 出现次数本身就是**会漏报的代理指标**——`AsyncDatabase::writer()` 返回 `&tokio_rusqlite::Connection`，`conn.execute(sql, [])` 不需要任何 `rusqlite::` 路径，`secret_store_crypto.rs` 因此有 4 条生产 SQL、0 处引用，只查 manifest 的门禁会报它干净。所以 A/B/C 三分法与真实依赖图不吻合，分层线必须画在数据库上。

  需求 4 的前提也不成立：scheduler 与 daemon 中**没有任何显式事务**，core 之外全部 11 处显式事务属于 slack-gateway（10，自有库）与 security（1，已豁免）。原文称为最难一类的事务边界接口在被禁止的一侧根本不需要——这使严格形态远比原文估计的便宜，而这一"便宜"是原文的错误前提掩盖掉的
- FR-135 已闭环删除；其 bash 3.2 危险构造的全仓清除与门禁（空数组展开、`declare -A`、`mapfile`/`readarray`、`${x^^}`、`local -n`、`wait -n`、`shopt -s globstar` 七类，扫描面由 `git ls-files '*.sh'` 得出、无豁免清单）、在真实 `/bin/bash` 下**执行**每一类而非仅匹配的 fixture 组、覆盖率脚本 shell 主路径的桩化冒烟（逐字断言 `cargo llvm-cov` argv，空与非空两种分支），以及产物上传步骤的诊断保真，现由 `docs/design_doc/orchestrator/146-bash32-compatibility.md`、`docs/qa/orchestrator/184-bash32-compatibility.md`、`scripts/qa/{bash32-compat.rb,test-bash32-compat.sh,test-coverage-governance-mainpath.sh}`、`config/governance/qa-gate-surface.json` 与 `.github/workflows/ci.yml` 承载。`boundary-coverage` 已在 run `30182768742` 首次成功，日志出现 `coverage governance passed` 并上传 3.9 MB 产物，`config/governance/ci-job-liveness.json` 的 `knownFailing` 标注随之移除。

  FR 原文的四处事实偏差：解释器**不是**来自 workflow 的 `shell: /bin/bash -e`（`ci.yml` 无 `defaults`、该 step 也未声明 `shell:`），而是脚本自身 `#!/usr/bin/env bash` 在 macOS runner 上解析到 3.2 —— 这决定了给 step 加 `shell: bash` 修不了任何东西，且暴露面是**所有** macOS job 执行的 shell 文件而非一个 step；`BASH_COMPAT=3.2` 对 bash 5.3 实测无法恢复其中任何一类语义，故语义半边只能托管在 macOS job 上，Linux runner 上必须如实报 skip；`mapfile` 并非仅是 FR-126 期间的历史形态，`.claude/skills/security-test-doc-gen/scripts/extract_surface.sh` 仍有 4 处，另有 FR 未提及的 `declare -A`（`scripts/qa/test-coordination-strangler.sh:154`）；朴素规则会误报的 `${!a[@]}` 与 `${#a[@]}` 在 bash 3.2 下**实测安全**，`scripts/regression/lib/probe-runner-lib.sh` 正是这一形态，不应改写。提交数"42"在 FR 撰写时精确（今为 77）。

  实施中另发现两项 FR 未预见者：第一处缺陷修复后，`boundary-coverage` 才跑到 `cargo llvm-cov` 里两分半，暴露出第二道从未被观察到的阻塞——`tauri::generate_context!` 在编译期读取 `frontendDist: ../../gui/dist`，而该 job 只做了 `npm ci` 从未构建前端 bundle，`orchestrator-gui` 在此根本无法编译；一个 job 因某一原因常红，会把第二个原因无限期地藏起来，"第一处错误已修"并不等于"这个 job 能跑"。其次，本门禁的 wrapper 自身也在被扫描面内，因此其全部 fixture 必须以 here-document 写出，且 builtin 规则必须要求命令位置——否则路径 `$WORK/hazard/mapfile.sh` 里的字样会被当成调用；case 8 原本自称测试"无法解析的脚本"，而其 fixture `if [ -z "$1" ; then` 在 `bash -n` 下合法，该 case 一直建立在一条不成立的前提上
- FR-134 已闭环删除；其**以执行事实取代文本存在性**的四项判定（由解析 workflow step 得出的接线真实性、逐 agent 关联的 bundle 钉死校验、真实执行并双向验证的 provider 遮蔽断言、全集减豁免的 stale-claim 扫描）、**由发现而非枚举得出的覆盖面**（`git ls-files '*.md'` 全集、递归的 `scripts/qa/**` 分类与 `supportFiles[]`、由 git index 中被追踪符号链接推导的镜像根、由解析 workflow 得出的 CI job 清单）、**门禁可运行性**（依赖一致性、workspace 范围、诊断保真、provider stub 覆盖）、CI 存活性台账与环境等价性门禁，以及共享 Rust 词法器，现由 `docs/design_doc/orchestrator/145-gate-surface-execution-truth.md`、`docs/qa/orchestrator/183-gate-surface-execution-truth.md`、`config/governance/ci-job-liveness.json`、`config/governance/qa-gate-surface.json`、`scripts/lib/{rust_lexer,workflow_model,manifest_model,ci_env,gate_preamble,provider_isolation}.*`、`scripts/qa/{ci-liveness.rb,test-ci-liveness.sh,test-ci-environment-parity.sh,test-qa-gate-surface.sh,test-skill-mirror-integrity.sh}` 与 `.github/workflows/ci.yml`（`governance` 与新增的 `ci-environment-parity` job、`./.github/actions/provider-stubs` 复合 action）承载。

  FR 原文的六处事实偏差：台账规模"45 = 45 / 12 ci-required"在开工时实为 53 = 53 / 20（FR-131 与 FR-132 在其撰写后各自接入门禁）；stale-claim 漏扫"83 个被追踪 Markdown"实为 41（FR-131 已取消追踪 36 个生成页）；"改为全集后需复核既有误报"实测为 **0 处**——`.agents`/`.cursor` 是符号链接，`git ls-files` 根本不会下降进镜像，故豁免清单以空清单发布，也因此需要一条专门证明豁免机制本身有效的 fixture；缺陷 Y 的"6 处 `monotonic` 表述"实为 8 处且其中 5 处行号已漂移，遗漏的三处是 DD-137 的治理小结、设计文档索引中 DD-137 那一行，以及 QA-175 的"the ledger remains exact and monotonic"（同时断言两条规则，其中一条已不成立）；`--write` 的 CI 识别面"两处重复"实为三处（FR-134 开启期间 `doc-lifecycle.rb` 又添了一份）；缺陷 V 的 `scripts/qa/lib/hidden-gate.sh` 并非假设——`scripts/qa/lib/slack-live-certification-lib.sh` 当时已被追踪且已完全不可见。

  实施中另发现三项 FR 未预见者：需求 12 的发现式规则立刻捕到 `1f5af317` 引入的 `.claude/skills/orchestrator-guide/orchestrator-guide`——一条指向自身、解析到从不存在的 `.claude/.claude` 的被追踪符号链接，六项镜像检查全部看不见它，已删除；需求 9 的"显然修法"（逐行正则剥离字面量）比缺陷本身更糟，它看不见 `item_generate.rs:199` 的跨行原始字符串 `r#"{"items": [`，会把该模块提前 245 行闭合并把测试夹具当作生产用量交给棘轮，使 `capturesOrJsonPath` 从 53 变成 60，因此"基线不变"是一条**双向**判据而非形式要求；补齐 ripgrep 后 `slack-certification-recorded` 的 ubuntu 腿首次真正执行到断言，随即暴露 `slack_cert_file_mode` 的 `stat -f '%Lp' || stat -c '%a'` 在 GNU coreutils 下 `-f` 意为 `--file-system`、会先向 stdout 打印文件系统块再让回退值追加其后——该门禁自建立起从未在 Linux 上跑到过这一行。

  FR-127 的实质交付（执行面 3→12、台账双向完整、发现红了整个 FR 周期的 `test-legacy-coordination-decommission.sh`）不受影响，故未撤销其闭环；其"46 个门禁只有 3 个在 CI"的立论则需补一句：那 3 个里至少 2 个是死的——被 job 引用、被调度、在日志中出现，却因所在 job 未装 ripgrep 而停在 `command -v` 前置检查，一条断言都没执行过。**接线了不等于在守**，这是 FR-127 所要终结的那句话的下一层
- FR-132 已闭环删除；其 DD/QA 生命周期 frontmatter 约定（`lifecycle` / `related_fr` / `superseded_by`）、全部受治理文档的一次性回填（回填时 378 篇；计入本 FR 自身产物后为 380 篇，377 active、3 superseded、244 篇带可考证的 FR 归属）、以真实 YAML 解析而非 `key: value` 正则读取前言、由文件系统而非名单推导覆盖面、对悬空/自指/成环的 `superseded_by` 的拒绝，以及双向精确比对的反向索引现由 `docs/design_doc/orchestrator/144-doc-lifecycle-governance.md`、`docs/qa/orchestrator/182-doc-lifecycle-governance.md`、`config/governance/doc-lifecycle-index.json`、`scripts/qa/doc-lifecycle.rb`、`scripts/qa/test-doc-lifecycle.sh` 与 `.github/workflows/ci.yml` 的 `governance` job 承载。FR 原文的四处偏差：字段名 `status` 与 71 篇 DD 已有的 `**Status**:`（Approved/Implemented/Released，含义是实现成熟度而非文档时效）冲突，二者是独立维度——DD-101 正是 `Released` 且已被取代，故改名为 `lifecycle`；背景段把 DD-127 列入需补历史围栏之列，实际它是 `**Status**: Released` 且被 DD-129/130/138 与 `docs/architecture.md` 引用为**现行**驱动抽象，其横幅是发布后更新而非取代围栏，真正被取代的恰是 DD-101/102/103 三篇；需求 4 的"豁免清单长度单调不增"是最弱形式（换一进一即可绕过，正是 FR-128 中 `capturesOrJsonPath` 54 对 55 的形态），故按用户决策取消豁免清单、以一次性回填完成；"生产代码行数 100213"无法复现（原始非测试 `.rs` 为 148733，按本仓库既有口径剔除 `cfg(test)` 后为 108710），文档:代码实为约 0.73:1 而非 0.86:1。此外原文未言明的起点是 150 篇 DD 中**零篇**带 YAML frontmatter，而 226 篇 QA 多数已有——两种互不兼容的编码，共约 172 处违规。均已在 DD-144 中留档。**2026-07-25 闭环后审计补正**：`DOC_ROOTS` 原为两个根，而 `docs/security/` 的 19 篇受治理文档 frontmatter 覆盖率为 0，门禁却始终报 12/12——绿是因为它们从未被看过。其中两篇正是 `FR-117` / `FR-117-A` 的闭环承载物，与 DD/QA 同等地位。根因在 FR 原文把范围写成"`docs/design_doc/**` 与 `docs/qa/**`"，实现忠实于一个过窄的需求。现已加入 `docs/security` 并回填 19 篇（只填 `lifecycle: active`，不从文中首个 FR 编号推断归属——那通常是引用而非作者，猜测会向反向索引注入 11 条未经审阅的归属），索引由 380 篇增至 399 篇、`byFeatureRequest` 保持 244，后者正是不猜的可见代价
- FR-129 已闭环删除；其镜像策略数据文件、逐镜像根的双向覆盖比对、规范符号链接形状校验、**逐个打开 `<root>/<name>/SKILL.md` 并要求非空常规文件的读取校验**（结构性检查全绿而仅该项失败的 fixture 4a 即为本 FR 缺陷的最小形态）、策略陈旧声明检测、"源树之外不得存在被追踪的 `SKILL.md`"的单一来源规则（删除只是一次性动作，该规则才是持久不变量），以及"检查不得从注册表中消失、亦不得无负向 fixture"的自校验现由 `docs/design_doc/orchestrator/141-skill-mirror-integrity.md`、`docs/qa/orchestrator/179-skill-mirror-integrity.md`、`config/governance/skill-mirrors.json`、`scripts/qa/test-skill-mirror-integrity.sh` 与 `.github/workflows/ci.yml` 的 `governance` job 承载。FR 原文的三处事实偏差（"30 个 skill" 实为 29 个 skill 加非 skill 的 `tools/`、`skills/orchestrator-guide/` 并非 `package-skills.sh` 产出而是无生产者、该副本非"容易漂移"而是已漂移约 32KB）与其遗漏的 `.cursor/skills` 第二镜像已在 DD-141 中留档
- FR-131 已闭环删除；其发布集合策略数据文件、逐 locale 双向的"声明 vs 产出"比对（由**真实运行生成器并 diff 目录树**得出，且期望集合独立于生成器推导，否则只能证明生成器与自己一致）、同步幂等性、导航可达性的双向校验（每条导航链接必须落在已产出页面上，每个已产出页面必须被导航链接——后者在修复前有 12 个页面不满足）、以及全仓相对链接解析门禁现由 `docs/design_doc/orchestrator/143-docs-publishing-integrity.md`、`docs/qa/orchestrator/181-docs-publishing-integrity.md`、`config/governance/docs-publishing.json`、`config/governance/markdown-links.json`、`scripts/qa/test-docs-publishing-integrity.sh`、`scripts/qa/test-markdown-link-integrity.sh` 与 `.github/workflows/ci.yml` 的 `governance` job 承载。FR 原文最危险的一处偏差是把 `site/*/showcases/` 当作 `docs/showcases/` 的"手工副本"并要求直接 gitignore——实际 `site/en` 是全仓唯一的英文 showcase 正文、`site/zh` 是链接改写过的中文，按原文执行会删除 17 篇英文与 1 篇中文译文，因此先做来源回收再做取消追踪；"已积累 3 处失效链接"实为 1 处（另两处分别位于行内代码块与围栏代码块中，本就不是链接），而 FR 未发现的 6 处无扩展名站点链接恰是朴素检查器会误报的形态，故该门禁的十条正向 fixture 才是主证据；此外 FR 未察觉 `docs.yml` 的 `paths:` 过滤器根本无法触发 showcase 变更、`sync-docs.mjs` 从不删除产物、12 个已发布页面无任何导航入口，以及其"不改动导航配置"的非目标与"缺失 showcase 须出现在站点"的验收标准自相矛盾。均已在 DD-143 中留档
- FR-130 的需求 1（边界冻结）与需求 3（迁移等价证明）已闭环，FR 文档保留并降为剩余的需求 2/4；其边界台账（core 的 52 `pub mod`、924 公开项、**逐文件** 200 处 `rusqlite` 引用、6 个直接依赖 rusqlite 的 crate）、双向精确比对的棘轮、37 条迁移产出的 46 表 + 92 索引 schema 基线，以及幂等性与全部 37 个中断点的续跑等价验证（治理期曾记作 74，那是 `grep -c m00` 的结果），现由 `docs/design_doc/orchestrator/142-core-boundary-freeze.md`、`docs/qa/orchestrator/180-core-boundary-freeze.md`、`config/governance/core-boundary-ledger.json`、`config/governance/schema-snapshot.sql`、`scripts/lib/rust_source.rb`、`scripts/qa/core-boundary.rb` 与 `scripts/qa/test-core-boundary.sh` 承载。FR 原文的关键偏差是需求 2 的文件清单（14 个文件 11049 行）与其验收标准"core 不再直接依赖 rusqlite"不相容——实际有 37 个文件 200 处引用，其中 18 个在同一函数内混装 SQL 与领域逻辑（原文写作约 22，与其自身表格不符）；另有"43 个 `too_many_arguments`"实为全工作区总数（core 仅 3 个）、公开项 742 漏计 `pub async fn`（实为 924）、51 张表实为 46 表 + 92 索引，均已在 DD-142 与重写后的 FR 中留档
- FR-128 已闭环删除；其台账再生模式（`--emit-inventory` / `--emit-baseline` / 拒绝在 CI 下执行的 `--write`）、与比对逻辑共用同一表达式的候选生成、逐 Agent 失配报告（新增/消失/变更及变化的 spec 顶层键，经 `git show HEAD:<file>` 还原被审阅 spec，并在台账与 spec 分离提交时自我诊断）、精确化的四项 source 棘轮与修正后的 `cfg(test)` 扫描口径现由 `docs/design_doc/orchestrator/140-governance-ledger-regeneration.md`、`docs/qa/orchestrator/178-governance-ledger-regeneration.md`、`scripts/qa/coordination-governance.rb`、`scripts/qa/test-governance-ledger-tooling.sh` 与 `CONTRIBUTING.md` 的审阅流程一节承载
- FR-127 已闭环删除；其门禁执行面分类、清单与磁盘双向比对、workflow 接线真实性校验、provider 隔离不变量、失效治理声明扫描与七个互相隔离的负向 fixture 现由 `docs/design_doc/orchestrator/139-qa-gate-enforcement-surface.md`、`docs/qa/orchestrator/177-qa-gate-enforcement-surface.md`、`config/governance/qa-gate-surface.json`、`scripts/qa/test-qa-gate-surface.sh` 与 `.github/workflows/ci.yml` 的 `governance` job 承载
- FR-125 已闭环删除；其精确消费者清单、capture/JSONPath 生产路径退役、窄化持久状态、显式兼容性阻塞项与退役后工具工作流证据现由 `docs/design_doc/orchestrator/137-legacy-coordination-decommission.md`、`docs/qa/orchestrator/175-legacy-coordination-decommission.md`、`config/governance/coordination-collapse-ledger.json` 与 `scripts/qa/test-legacy-coordination-decommission.sh` 承载
- FR-124 已闭环删除；其 11 个生产 Workflow 精确分类、7 个非 governance-only 迁移、逐工作流 legacy/tool 对等证据、显式 tool/session 边界、`record_metric`、self-bootstrap 两周期生存回归、冻结棘轮与三级退役标准现由 `docs/design_doc/orchestrator/136-coordination-strangler-completion.md`、`docs/qa/orchestrator/174-coordination-strangler-completion.md`、`config/governance/coordination-collapse-ledger.json`、`fixtures/manifests/bundles/coordination-strangler-parity.yaml` 与 `scripts/qa/test-coordination-strangler.sh` 承载
- FR-123 已闭环删除；其 shared/dedicated/组合认证入口、可恢复 provider checkpoint、inert 最小 secret 环境、同消息双 badge smoke、recorded provider CI、可过期安全证据、README/release 状态与显式清理 inventory 现由 `docs/design_doc/orchestrator/135-slack-sandbox-continuous-certification.md`、`docs/qa/orchestrator/173-slack-sandbox-continuous-certification.md`、`docs/guide/slack-managed-sandbox-certification-runbook.md`、`docs/qa/evidence/slack-live-certification-latest.json` 与 `scripts/qa/certify-slack-managed-live.sh` 承载
- FR-122 已闭环删除；其统一覆盖率命令、批准基线非回退门禁、Rust branch `unsupported` 语义、五类 daemon 边界矩阵、CLI/Tauri 真实 gRPC 适配器模板及 FR-095～FR-118 证据索引现由 `docs/design_doc/orchestrator/134-boundary-layer-coverage-governance.md`、`docs/qa/orchestrator/172-boundary-layer-coverage-governance.md`、`coverage/boundary-baseline.json`、`coverage/README.md` 与 `scripts/coverage-governance.sh` 承载
- FR-121 已闭环删除；其独立查询/流/mutation 错误生命周期、统一失败对账、持久可访问 alert、安全错误边界、焦点恢复、隐私安全指标与双客户端竞争证据现由 `docs/design_doc/orchestrator/133-attention-mutation-error-reconciliation.md`、`docs/qa/orchestrator/171-attention-mutation-error-reconciliation.md` 与 `scripts/qa/test-attention-inbox.sh` 承载
- FR-118 已闭环删除；其 authenticated daemon tool host、transport-only stdio shim、五个真实协调工具、完整事件证据、pilot parity、协调行数塌缩与残余跨步通道度量现由 `docs/design_doc/orchestrator/130-coordination-collapse-mcp-tools.md`、`docs/qa/orchestrator/168-coordination-collapse-mcp-tools.md`、`docs/guide/coordination-tools.md`、`fixtures/manifests/bundles/coordination-collapse-pilot.yaml` 与 `scripts/qa/test-coordination-collapse.sh` 承载
- FR-117 已闭环删除；其 task Workspace、隐式 process item、FileSharing 天花板、HOME/XDG 隔离、全局 Skill 只读访问、Console 语义与 Slack inventory pilot 证据现由 `docs/design_doc/orchestrator/128-non-code-workspace-and-global-file-sharing.md`、`docs/qa/orchestrator/165-non-code-workspace-and-global-file-sharing.md`、`docs/security/authorization/02-file-sharing-ceiling.md`、`docs/security/file-security/02-workspace-home-isolation.md`、`docs/guide/non-code-workspace.md` 与 `scripts/qa/test-non-code-workspace.sh` 承载
- FR-117-A 已闭环删除；其 daemon UID、group/world 权限位、task-writable 路径重叠与跨平台 fail-closed 结论现由 `docs/design_doc/orchestrator/128-non-code-workspace-and-global-file-sharing.md`、`docs/qa/orchestrator/167-global-skill-directory-provenance.md` 与 `docs/security/authorization/02-file-sharing-ceiling.md` 承载
- FR-116 已闭环删除；其 driver 契约、三种 CLI provider、能力门禁、直接事件折叠、session 隐私、MCP 隔离与 shell pilot 证据现由 `docs/design_doc/orchestrator/127-agent-driver-abstraction.md`、`docs/qa/orchestrator/164-agent-driver-abstraction.md`、`docs/guide/agent-driver-model.md`、`fixtures/manifests/bundles/agent-driver-fixture.yaml` 与 `scripts/qa/test-agent-driver-abstraction.sh` 承载
- FR-116-A 已闭环删除；其 Codex CLI `0.144.5` resume 命令、同 thread 上下文继承、recorded JSONL 映射、session/认证隔离与版本漂移复验信息现由 `docs/design_doc/orchestrator/129-codex-session-resume-conformance.md`、`docs/qa/orchestrator/166-codex-session-resume-conformance.md`、`fixtures/driver/codex-cli-0.144.5-resume.json`、`scripts/qa/test-codex-session-resume.sh` 与 `scripts/qa/certify-codex-session-resume.sh` 承载
- FR-115 已闭环删除；其 per-workspace private App provisioning、local-only Configuration Token、receipt-gated credential import、exact-App OAuth/events、reviewed lifecycle/migration、same-message two-badge routing、离线 cursor 恢复与受控 Slack sandbox 清理证据现由 `docs/design_doc/orchestrator/126-dedicated-slack-app-auto-provisioning.md`、`docs/qa/orchestrator/163-dedicated-slack-app-auto-provisioning.md`、`docs/guide/slack-dedicated-app-provisioning.md` 与 `scripts/qa/test-slack-dedicated-app-provisioning.sh` 承载
- FR-114 已闭环删除；其 shared official App OAuth、SourceConnection/Gateway 边界、双 workspace/daemon live certification、恢复/转移/撤销/备份证据与可复跑 harness 现由 `docs/design_doc/orchestrator/125-managed-slack-connection-shared-oauth.md`、`docs/qa/orchestrator/162-managed-slack-connection-shared-oauth.md`、`docs/guide/slack-managed-sandbox-certification-runbook.md`、`scripts/qa/test-slack-managed-shared-oauth.sh` 与 `scripts/qa/certify-slack-managed-live.sh` 承载
- FR-113 已闭环删除；其 clean-tree aggregate、双 badge 垂直链路、并发/重启恢复、真实 Tauri 边界、前向升级/兼容回滚、用户指南与隐私诊断证据现由 `docs/design_doc/orchestrator/124-slack-reaction-skill-automation-release.md`、`docs/qa/orchestrator/161-slack-reaction-skill-automation-release.md`、`docs/guide/slack-reaction-skill-automation.md`、`fixtures/manifests/bundles/slack-skill-automation-release-fixture.yaml` 与 `scripts/qa/test-slack-skill-automation-release.sh` 承载
- FR-111 已闭环删除；其 bounded retry/lease recovery、Attention 恢复、安全查询/模拟/重放、暂停投影、隐私安全指标与保留策略现由 `docs/design_doc/orchestrator/122-source-automation-reliability-operations.md`、`docs/qa/orchestrator/159-source-automation-reliability-operations.md` 与 `scripts/qa/test-source-automation-operations.sh` 承载
- FR-110 已闭环删除；其 Slack outbound credential、permalink 验证、durable automation route、canonical task/audit、幂等重启收敛与角色感知深链现由 `docs/design_doc/orchestrator/121-slack-permalink-canonical-task-routing.md`、`docs/qa/orchestrator/158-slack-permalink-canonical-task-routing.md`、`fixtures/manifests/bundles/source-task-routing-fixture.yaml` 与 `scripts/qa/test-slack-reaction-task-routing.sh` 承载
- FR-109 已闭环删除；其 native SourceTaskBinding、确定性匹配、冲突回滚、热更新、引用治理与可复现证据现由 `docs/design_doc/orchestrator/120-source-task-binding-badge-matching.md`、`docs/qa/orchestrator/157-source-task-binding-badge-matching.md`、`fixtures/manifests/bundles/source-task-binding-fixture.yaml` 与 `scripts/qa/test-source-task-binding.sh` 承载
- FR-108 已闭环删除；其 native SourceTaskTemplate、确定性安全预览、热更新/重启、引用治理与可复现证据现由 `docs/design_doc/orchestrator/119-source-task-template-skill-invocation.md`、`docs/qa/orchestrator/156-source-task-template-skill-invocation.md`、`fixtures/manifests/bundles/source-task-template-fixture.yaml` 与 `scripts/qa/test-source-task-template.sh` 承载
- FR-107 已闭环删除；其 provider-neutral reaction contract、Slack normalization、非变更路由闸门与 Sources 验证现由 `docs/design_doc/orchestrator/118-slack-reaction-source-event-contract.md`、`docs/qa/orchestrator/155-slack-reaction-source-event-contract.md` 与 `scripts/qa/test-slack-reaction-source.sh` 承载
- FR-098 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/108-agent-session-control-plane.md`、`docs/qa/orchestrator/145-agent-session-control-plane.md` 与 `fixtures/manifests/bundles/session-control-mock.yaml` 承载
- FR-095 至 FR-106 已全部闭环为 DD/QA、运维手册与可执行验收产物；Console v1 已达到 release-complete，整体状态与证据由 `docs/design_doc/orchestrator/116-process-console-release-acceptance.md`、`docs/qa/orchestrator/153-process-console-release-acceptance.md`、`docs/guide/agent-process-console-v1-operations.md` 与 `scripts/qa/test-process-console-release.sh` 承载
- FR-106 已闭环删除；其 migration identity、populated upgrade、聚合发布门禁和 forward-only 运维/回滚证据现由 `docs/design_doc/orchestrator/116-process-console-release-acceptance.md`、`docs/qa/orchestrator/153-process-console-release-acceptance.md`、`docs/guide/agent-process-console-v1-operations.md` 与 `scripts/qa/test-process-console-release.sh` 承载
- FR-105 已闭环删除；其确定性 `_system` RuntimePolicy 权威语义、热更新/重启验证与 Session 回归证据现由 `docs/design_doc/orchestrator/115-session-runtime-policy-authority.md`、`docs/qa/orchestrator/152-session-runtime-policy-authority.md` 与 `scripts/qa/test-agent-session-control-plane.sh` 承载
- FR-104 已闭环删除；其精确产品指标、本地运营视图、投影健康、生命周期与性能证据现由 `docs/design_doc/orchestrator/114-process-console-operational-metrics.md`、`docs/qa/orchestrator/151-process-console-operational-metrics.md` 与 `scripts/qa/test-process-console-metrics.sh` 承载
- FR-103 已闭环删除；其审核恢复、Attention 通知、真实 Tauri/gRPC 垂直验收与可访问性证据现由 `docs/design_doc/orchestrator/113-process-console-recovery-notifications-e2e.md`、`docs/qa/orchestrator/150-process-console-recovery-notifications-e2e.md` 与 `scripts/qa/test-process-console-vertical-flow.sh` 承载
- FR-101 已闭环删除；其统一 Action Audit Envelope、迁移/兼容设计与可复现验证现由 `docs/design_doc/orchestrator/111-control-plane-action-audit-envelope.md`、`docs/qa/orchestrator/148-control-plane-action-audit-envelope.md` 与 `scripts/qa/test-control-plane-action-audit.sh` 承载
- FR-102 已闭环删除；其 Session Control Plane 硬化、迁移/重启/并发/RBAC/GUI 验收现由 `docs/design_doc/orchestrator/112-agent-session-control-plane-hardening.md`、`docs/qa/orchestrator/149-agent-session-control-plane-hardening.md` 与 `scripts/qa/test-agent-session-control-plane.sh` 承载
- FR-100 已闭环删除；其信息架构、迁移/回滚设计与可复现 UI 验证现由 `docs/design_doc/orchestrator/110-process-console-information-architecture.md`、`docs/qa/orchestrator/147-process-console-ui.md` 与 `scripts/qa/test-process-console-ui.sh` 承载
- FR-097 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/107-handoff-and-safe-resume.md`、`docs/qa/orchestrator/144-handoff-and-safe-resume.md` 与 `scripts/qa/test-handoff-safe-resume.sh` 承载
- FR-099 已闭环删除；其设计、验证与 deterministic fixture 现由 `docs/design_doc/orchestrator/109-source-events-and-slack-binding.md`、`docs/qa/orchestrator/146-source-events-and-slack-binding.md`、`fixtures/manifests/bundles/source-events-fixture.yaml` 与 `scripts/qa/test-source-events-slack.sh` 承载
- FR-096 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/106-attention-inbox.md`、`docs/qa/orchestrator/143-attention-inbox.md` 与 `scripts/qa/test-attention-inbox.sh` 承载
- FR-095 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/105-process-timeline-read-model.md`、`docs/qa/orchestrator/142-process-timeline-read-model.md` 与 `scripts/qa/test-process-timeline.sh` 承载
- FR-011 聚焦内核复杂度治理，不直接引入用户可见新能力
- FR-012 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/27-secretstore-key-lifecycle.md` 与 `docs/qa/orchestrator/64-secretstore-key-lifecycle.md` 承载
- FR-013 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/27-grpc-control-plane-protection.md`、`docs/qa/orchestrator/65-grpc-control-plane-protection.md` 与 `scripts/qa/test-fr013-control-plane-protection.sh` 承载
- FR-014 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/28-error-semantics-governance.md` 与 `docs/qa/orchestrator/66-error-semantics-governance.md` 承载
- FR-015 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/29-clone-reduction-and-shared-ownership.md`、`docs/qa/orchestrator/67-clone-reduction-and-shared-ownership.md` 与 `docs/qa/orchestrator/68-clone-reduction-follow-up.md` 承载
- FR-016 已闭环删除；其设计、验证与门禁信息现由 `docs/design_doc/orchestrator/30-async-lock-model-alignment.md`、`docs/qa/orchestrator/69-async-lock-model-alignment.md` 与 `scripts/check-async-lock-governance.sh` 持续承载
- FR-017 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/agent-drain-enabled.md` 与 `docs/qa/orchestrator/agent-drain-enabled.md` 承载
- FR-018 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/guide-alignment.md` 与 `docs/qa/orchestrator/guide-alignment.md` 承载，`guide-alignment` skill 提供持续治理能力
- FR-019 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/31-libc-cross-platform-compilation.md` 与 `docs/qa/orchestrator/70-libc-cross-platform-compilation.md` 承载
- FR-009 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/25-database-persistence-bootstrap-repositories.md`、`docs/design_doc/orchestrator/26-database-migration-kernel-and-repository-governance.md`、`docs/qa/orchestrator/62-database-persistence-bootstrap-repositories.md` 与 `docs/qa/orchestrator/63-database-migration-kernel-and-repository-governance.md` 承载
- FR-008 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/13-unified-step-execution-model.md`、`docs/guide/**` 与 `docs/qa/orchestrator/61-chain-steps-execution.md` 承载
- FR-007 已闭环删除；其收口结果由 `docs/architecture.md`、`docs/guide/**`、`.claude/skills/orchestrator-guide/**` 与 `docs/qa/**` 持续承载
- FR-006 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/21-sandbox-resource-network-enforcement.md` 与 `docs/qa/orchestrator/56-sandbox-resource-network-enforcement.md` 承载
- FR-010 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/22-control-plane-security.md` 与 `docs/qa/orchestrator/58-control-plane-security.md` 承载
- FR-020 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/32-automate-protoc-dependency.md` 与 `docs/qa/orchestrator/71-automate-protoc-dependency.md` 承载
- FR-021 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/33-audit-reduce-expect-calls.md` 与 `docs/qa/orchestrator/72-audit-reduce-expect-calls.md` 承载
- FR-022 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/36-public-api-doc-comments.md` 与 `docs/qa/orchestrator/75-public-api-doc-comments.md` 承载
- FR-023 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/34-integration-test-coverage.md` 与 `docs/qa/orchestrator/73-integration-test-coverage.md` 承载
- FR-024 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/35-audit-unsafe-blocks.md` 与 `docs/qa/orchestrator/74-audit-unsafe-blocks.md` 承载
- FR-025 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/37-config-load-module-split.md` 与 `docs/qa/orchestrator/76-config-load-module-split.md` 承载
- FR-027 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/39-worker-notify-wakeup.md` 与 `docs/qa/orchestrator/78-worker-notify-wakeup.md` 承载
- FR-028 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/40-benchmark-score-capture.md` 与 `docs/qa/orchestrator/79-benchmark-score-capture.md` 承载
- FR-026 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/38-event-table-ttl-archival.md` 与 `docs/qa/orchestrator/77-event-table-ttl-archival.md` 承载
- FR-029 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/41-item-scoped-git-worktree-isolation.md` 与 `docs/qa/orchestrator/80-item-scoped-git-worktree-isolation.md` 承载
- FR-030 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/42-self-evolution-db-schema-alignment.md` 与 `docs/qa/orchestrator/81-self-evolution-db-schema-alignment.md` 承载
- FR-034 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/44-self-referential-daemon-pid-guard.md` 与 `docs/qa/orchestrator/87-self-referential-daemon-pid-guard.md` 承载
- FR-035 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/12-degenerate-cycle-loop-guard.md` 与 `docs/qa/orchestrator/88-degenerate-cycle-loop-guard.md` 承载
- FR-036 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/45-plan-output-context-overflow-mitigation.md` 与 `docs/qa/orchestrator/89-plan-output-context-overflow-mitigation.md` 承载
- FR-031 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/46-unquoted-json-extraction.md` 与 `docs/qa/orchestrator/90-unquoted-json-extraction.md` 承载
- FR-032 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/47-daemon-crash-resilience.md` 与 `docs/qa/orchestrator/91-daemon-crash-resilience.md` 承载
- FR-033 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/48-orphaned-running-items-recovery.md` 与 `docs/qa/orchestrator/86-orphaned-running-items-recovery.md` 承载
- FR-037 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/49-dynamic-items-cycle-overflow.md` 与 `docs/qa/orchestrator/92-dynamic-items-cycle-overflow.md` 承载
- FR-038 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/50-inflight-step-completion-race.md` 与 `docs/qa/orchestrator/93-inflight-step-completion-race.md` 承载
- FR-039 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/51-trigger-resource-cron-event-driven-task-creation.md` 与 `docs/qa/orchestrator/94-trigger-resource-cron-event-driven.md` 承载
- FR-040 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/52-prehook-self-referential-safe-filter.md` 与 `docs/qa/orchestrator/95-prehook-self-referential-safe-filter.md` 承载
- FR-041 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/53-self-restart-socket-continuity.md` 与 `docs/qa/orchestrator/96-self-restart-socket-continuity.md` 承载
- FR-042 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/54-follow-task-logs-callback.md` 与 `docs/qa/orchestrator/97-follow-task-logs-callback.md` 承载
- FR-043 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/55-convergence-expression.md` 与 `docs/qa/orchestrator/98-convergence-expression.md` 承载
- FR-002 已闭环；其设计与验证信息现由 `docs/design_doc/orchestrator/22-control-plane-security.md` 与 `docs/qa/orchestrator/58-control-plane-security.md` 承载（mTLS、RBAC 授权、审计日志均已实现）
- FR-005 已闭环；其设计与验证信息现由 `docs/design_doc/orchestrator/24-daemon-lifecycle-runtime-metrics.md` 与 `docs/qa/orchestrator/60-daemon-lifecycle-runtime-metrics.md` 承载（生命周期状态机、运行时指标、优雅 drain 均已实现）
- FR-011 已闭环；代码已自然实现 validate/scheduler/runner 的职责分离（config_load/validate/、output_validation.rs、runner/sandbox.rs 各司其职），无需进一步重构
- FR-044 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/56-sandbox-denial-detection.md` 与 `docs/qa/orchestrator/56-sandbox-denial-anomaly-trace.md` 承载
- FR-045 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/57-long-lived-command-guard.md` 与 `docs/qa/orchestrator/99-long-lived-command-guard.md` 承载
- FR-046 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/58-agent-subprocess-daemon-pid-guard.md` 与 `docs/qa/orchestrator/100-agent-subprocess-daemon-pid-guard.md` 承载
- FR-047 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/59-core-crate-split-config.md` 与 `docs/qa/orchestrator/101-core-crate-split-config.md` 承载
- FR-048 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/60-core-crate-split-scheduler.md` 与 `docs/qa/orchestrator/102-core-crate-split-scheduler.md` 承载
- FR-049 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/61-prehook-pipeline-vars.md` 与 `docs/qa/orchestrator/103-prehook-pipeline-vars.md` 承载
- FR-050 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/62-cli-uds-fallback-robustness.md` 与 `docs/qa/orchestrator/104-cli-uds-fallback-robustness.md` 承载
- FR-051 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/63-workflow-yaml-unknown-field-warning.md` 与 `docs/qa/orchestrator/105-workflow-yaml-unknown-field-warning.md` 承载
- FR-052 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/64-inflight-wait-heartbeat-aware-timeout.md` 与 `docs/qa/orchestrator/106-inflight-wait-heartbeat-aware-timeout.md` 承载
- FR-053 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/65-parallel-dispatch-completeness-guard.md` 与 `docs/qa/orchestrator/107-parallel-dispatch-completeness-guard.md` 承载
- FR-054 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/66-incremental-item-progress.md` 与 `docs/qa/orchestrator/108-incremental-item-progress.md` 承载
- FR-055 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/67-parallel-spawn-stagger-delay.md` 与 `docs/qa/orchestrator/109-parallel-spawn-stagger-delay.md` 承载
- FR-056 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/68-agent-health-policy-configuration.md` 与 `docs/qa/orchestrator/110-agent-health-policy-configuration.md` 承载
- FR-057 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/69-daemon-proper-daemonize.md` 与 `docs/qa/orchestrator/111-daemon-proper-daemonize.md` 承载
- FR-058 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/70-scenario-level-self-referential-safety.md` 与 `docs/qa/orchestrator/112-scenario-level-self-referential-safety.md` 承载
- FR-061 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/71-logging-env-var-override.md` 与 `docs/qa/orchestrator/113-logging-env-var-override.md` 承载
- FR-062 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/72-agent-health-state-observability.md` 与 `docs/qa/orchestrator/114-agent-health-state-observability.md` 承载
- FR-065 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/75-agent-mailbox-session-communication.md` 与 `docs/qa/orchestrator/115-agent-mailbox-session-communication.md` 承载
- FR-063 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/73-gui-architecture-tauri-grpc.md` 与 `docs/qa/orchestrator/116-gui-architecture-tauri-grpc.md` 承载
- FR-064 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/74-gui-uiux-wish-pool-progress.md` 与 `docs/qa/orchestrator/117-gui-uiux-wish-pool-progress.md` 承载
- FR-066 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/76-gui-realtime-wish-isolation.md` 与 `docs/qa/orchestrator/118-gui-realtime-wish-isolation.md` 承载
- FR-060 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/77-reduce-unsafe-qa-operations.md` 承载（13 次迭代将 unsafe 文档从 114 降至 33，+360 安全场景，23.1% unsafe 率达成 < 30% 目标）
- FR-067 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/77-gui-cli-rpc-parity.md` 与 `docs/qa/orchestrator/119-gui-cli-rpc-parity.md` 承载
- FR-068 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/78-gui-connection-resilience-notification.md`、`docs/qa/orchestrator/120-gui-connection-resilience.md` 与 `docs/qa/orchestrator/120b-gui-notification-error-humanization.md` 承载
- FR-069 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/79-gui-polish-theme-i18n-responsive.md`、`docs/qa/orchestrator/121-gui-polish-visual.md` 与 `docs/qa/orchestrator/121b-gui-i18n-ux.md` 承载
- FR-070 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/80-evo-apply-winner-observability.md` 与 `docs/qa/orchestrator/122-evo-apply-winner-observability.md` 承载
- FR-071 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/81-open-source-compliance.md` 与 `docs/qa/orchestrator/123-open-source-compliance.md` 承载（LICENSE、CHANGELOG、CONTRIBUTING、GitHub 模板已就绪；v0.1.0 release 待 tag 推送）
- FR-072 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/82-homebrew-tap-distribution.md` 与 `docs/qa/orchestrator/124-homebrew-tap-distribution.md` 承载（Docker 分发因架构不兼容已排除——orchestratord 以子进程方式 spawn agent，需宿主机工具与凭证；已实现 Homebrew tap 与 cargo install 两条分发渠道）
- FR-073 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/83-documentation-site.md` 与 `docs/qa/orchestrator/125-documentation-site.md` 承载（VitePress 文档站 + Landing Page + "Why Orchestrator?" 对比页；README 精简至 74 行；Cloudflare Pages 自动部署）
- FR-078 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/84-task-items-event-list-cli.md` 与 `docs/qa/orchestrator/126-task-items-event-list-cli.md` 承载（新增 `task items` 和 `event list` CLI 命令，消除 showcase 中的 sqlite 直接查询）
- FR-079 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/85-data-lifecycle-governance.md` 与 `docs/qa/orchestrator/127-data-lifecycle-governance.md` 承载（日志 TTL 默认 30 天自动清理、`db vacuum` 命令、`db cleanup` 命令、`db status` 显示磁盘用量、可选 task 自动清理）
- FR-080 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/86-webhook-trigger-infrastructure.md` 与 `docs/qa/orchestrator/128-webhook-trigger-infrastructure.md` 承载（HTTP webhook 端点、`source: webhook` 触发器、HMAC 签名验证、`trigger fire --payload`、axum HTTP 服务与 gRPC 并行运行）
- FR-081 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/87-per-trigger-webhook-auth.md` 与 `docs/qa/orchestrator/129-per-trigger-webhook-auth-cel-filter.md` 承载（Per-trigger SecretStore 签名验证 + 多密钥轮替、自定义签名 header、CEL payload 过滤、全局 secret fallback）
- FR-082 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/88-integration-manifest-packages.md` 与 `docs/qa/orchestrator/130-integration-manifest-packages.md` 承载（`c9r-io/orchestrator-integrations` 独立仓库，Slack/GitHub/LINE 集成包，密钥轮替 showcase）
- FR-084 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/89-agent-command-rules-step-vars.md` 与 `docs/qa/orchestrator/100-agent-command-rules-step-vars.md` 承载（Agent `command_rules` CEL 条件命令选择、Step `step_vars` 临时变量覆盖、`command_rule_index` 审计列；Session 复用为纯 workflow 编排示例）
- FR-077 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/90-workflow-template-library.md` 与 `docs/qa/orchestrator/131-workflow-template-library.md` 承载（5 个渐进复杂度模板：hello-world / qa-loop / plan-execute / scheduled-scan / fr-watch，echo agent 零成本运行，文档站 Templates 分组）
- FR-085 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/91-filesystem-trigger.md` 与 `docs/qa/orchestrator/132-filesystem-trigger.md` 承载（`source: filesystem` 原生触发器，`notify` crate 跨平台文件监控，按需启停 watcher，路径白名单 + 事件类型 + 防抖 + CEL 四层过滤，macOS symlink 兼容）
- FR-086 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/94-agent-selection-threshold-verification.md` 与 `docs/qa/orchestrator/110b-agent-health-policy-advanced.md` 承载（采用 Option 2 单元测试验证路径：`is_capability_healthy_custom_threshold` + `test_diseased_agent_with_passing_capability_threshold_is_selected` 确定性证明 diseased agent 在自定义 `capability_success_threshold` 下的选中行为）
- FR-086（原 daemon config hot reload 议题）已闭环；其设计与验证信息现由 `docs/design_doc/orchestrator/92-daemon-config-hot-reload.md` 与 `docs/qa/orchestrator/133-daemon-config-hot-reload.md` 承载（ArcSwap 原子快照机制实现无重启配置热加载，`persist_config_and_reload()` 在 apply 响应前同步更新 `config_runtime`；QA 128 S2/S3 限制已移除）
- QA-106 inflight wait test fixture 已闭环删除（原 FR-085 编号冲突）；其设计与验证信息现由 `docs/design_doc/orchestrator/93-long-running-agent-test-fixture.md` 与 `docs/qa/orchestrator/106-inflight-wait-heartbeat-aware-timeout.md` 承载（3 项集成测试直接验证 `wait_for_inflight_runs()` 的 heartbeat 重置、超时回收、诊断事件；S1-S5 全部 ☑）
- FR-088 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/qa-doctor-observability.md` 与 `docs/qa/orchestrator/134-qa-doctor-observability.md` 承载
- FR-089 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/95-secretstore-key-emergency-recovery.md` 与 `docs/qa/orchestrator/135-secretstore-key-emergency-recovery.md` 承载
- FR-083 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/96-crd-plugin-system.md` 与 `docs/qa/orchestrator/136-crd-plugin-system.md` 承载（通用 CRD 插件系统：interceptor/transformer/cron 三种插件类型，webhook.authenticate/webhook.transform 扩展点，crdRef 触发器关联，内置 orchestrator tool 工具库）
- FR-087 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/97-agent-health-policy-cli-fixtures.md` 与 `docs/qa/orchestrator/110b-agent-health-policy-advanced.md` 承载（经审计确认 fixture manifest + `orchestrator apply --project` 完整管线已正确保留 health_policy，新增 `scripts/qa/test-health-policy-check.sh` 自动化验证 3 场景：自定义阈值、disease DISABLED、默认策略基线）
- FR-090 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/98-lightweight-step-run.md` 与 `docs/qa/orchestrator/138-lightweight-step-run.md` 承载（三阶段轻量化执行：Phase 1 `--step`/`--set` 步骤过滤与变量注入、Phase 2 `orchestrator run` 同步执行命令、Phase 3 `RunStep` RPC 脱离 workflow 直接组装单步执行）
- FR-091 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/99-linux-sandbox-filesystem-isolation.md` 与 `docs/qa/orchestrator/139-linux-sandbox-filesystem-isolation.md` 承载（Linux mount namespace + bind-mount 文件系统隔离：workspace_readonly / workspace_rw_scoped 两种模式，unshare -m 组合网络命名空间，preflight 动态检测 unshare/mount 二进制）
- FR-092 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/configurable-spill-path.md` 与 `docs/qa/orchestrator/100-configurable-spill-path.md` 承载（Workspace 级 `artifacts_dir` 配置，spill 文件写入工作区内，沙箱 agent 可访问）
- FR-093 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/sandbox-readable-paths.md` 与 `docs/qa/orchestrator/101-sandbox-readable-paths.md` 承载（ExecutionProfile `readable_paths` 字段、`~`/`$VAR` 路径展开、Linux bind-mount RO 挂载、`ORCHESTRATOR_READABLE_PATHS` 环境变量供 agent wrapper 消费）
- FR-094 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/step-scope-roundtrip-leak.md` 与 `docs/qa/orchestrator/141-step-scope-roundtrip-leak.md` 承载（自定义 step id 的显式 scope 跨 spec↔config 往返漂移修复：`resolved_scope` capability fallback 限定为 conventions 已知 id、`workflow_step_config_to_spec` 不再省略默认值优化、`task_ops::resolve_task_targets` 在 `QaDirectoryScan` 触发时发出 `qa_directory_scan_triggered` info 事件，超过 50 个 item 升级为 `qa_directory_scan_oversize` warning；6 个回归单测覆盖三层修复 + 一个端到端 round-trip dry-run，复制了 v3 retest 中 D1/E1 的 180-item 爆炸场景）
- FR-119 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/131-expert-resources-governed-editing.md` 与 `docs/qa/orchestrator/169-expert-resources-governed-editing.md` 承载（五类 daemon 权威资源目录、可应用 canonical Describe、受审核 revision fence Apply、Action Audit 隐私与可访问 Expert UI 均已闭环）
- FR-120 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/132-handoff-dialog-focus-lifecycle.md` 与 `docs/qa/orchestrator/170-handoff-dialog-focus-lifecycle.md` 承载（手动与 Attention 自动审查入口、焦点围栏与确定性恢复、异步失效防护、失败可操作性和 Chromium 可访问性均已闭环）
- FR-126 已经第四轮严格审计补证后重新闭环删除；mark-done showcase 已对齐 `claude/cli` typed-driver 当前事件与 artifact，全部 `docs/showcases/**/*.md` 进入退役语义扫描，EN/ZH 指南下游链接与正向语义由确定性门禁验证。设计与验证证据由 `docs/design_doc/orchestrator/138-agent-driver-execution-migration.md`、`docs/design_doc/orchestrator/guide-alignment.md`、`docs/qa/orchestrator/176-agent-driver-execution-migration.md` 与 `docs/qa/orchestrator/guide-alignment.md` 承载。
- FR-133 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/156-dependency-policy-gate.md` 与 `docs/qa/orchestrator/194-dependency-policy-gate.md` 承载（`deny.toml` 把依赖图的形状变成一项决定：48 个多版本 crate、70 条带理由的接受项，不使用 `skip-tree`；九条许可证表达式覆盖全部 653 个外部包，`target-lexicon` 一条例外；`sources` 当前干净，作为护栏交付。`cargo-deny` 与 `cargo audit` 按"一个问题一个工具"划分——实测两者互不包含，`cargo audit` 报告的 RUSTSEC-2024-0429（glib unsound）`cargo deny` 完全不报，故 `cargo audit --deny unsound` 改为阻断，接受项进入 `.cargo/audit.toml`。另新增 `scripts/qa/dependency-policy.rb`：`cargo deny` 证明策略成立，它证明策略仍然绑定）
- FR-159 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/171-interactive-session-process-reclamation.md` 与 `docs/qa/orchestrator/209-interactive-session-process-reclamation.md` 承载（协调器接上 OS 层回收：transport 消失的会话按进程组回收、删除其自有目录、并把决定——含拒绝——记为 `session_process_reclaimed` 事件；三项前置条件在紧邻发信号处重新求值，其中 `getpgid(pid) == pid` 为 FR 未写、实施期补入：fingerprint 证明的是进程身份而非组长身份。`session_reclaim_enabled` 默认 true 且刻意不挂在默认为 false 的 `session_control_enabled` 之下。实施期核实推翻了 FR 的三处判断：其一，调度器的六条 kill 路径全都够不到会话——tty 子进程从未进入 `runtime.child`，唯一的回收者 `kill_on_drop` 是单 PID 而非进程组，这正是止血记录里「组长已死、组内仍活」的成因；其二，FR 的首条验收判据与 DD-112 §54/§35 的重启可恢复契约直接矛盾，按 ppid 判定孤儿会删掉一个既有特性——实测使重启场景以 `session is not attachable` 失败，故保留 FR 需求 1 自己写的 transport 判据；其三，`cleanup_stale_sessions` 零生产调用点，其「先放弃回收再销毁证据」是潜伏而非在途缺陷。会话 stdin 由 `<` 改为 `0<>`：`write_fifo_atomically` 每次 send-input 都开-写-关，只读重定向下阻塞式 read 在第一条消息后即退出——mock fixture 的 `sleep 0.05` 忙等一直是在绕开这条传输缺陷，代价约每个孤儿进程 315 分钟 CPU。临时目录泄漏共 18102 项约 12 GB，其中 6047 项 FR 的清点方法看不见——它按 `agent_orchestrator.db` 这一个文件名且只数目录；改按前缀形状重新推导才拿全。10843 份的根因不是 Drop 失效而是 `db_write.rs` 一处有注释的 `mem::forget`，38 个调用点对 `make_test_db` 的 14 个，恰为 FR 观测到的 19:7）
- FR-076 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/165-gui-ci-integration.md`、`docs/design_doc/orchestrator/166-gui-release-packaging.md` 与 `docs/qa/orchestrator/203-gui-ci-integration.md`、`docs/qa/orchestrator/204-gui-release-packaging.md` 承载（需求 1：clippy/test job 就地拓宽纳入 GUI crate，负向验证以 throwaway 分支 dispatch 证明编译错误使 CI 失败；需求 2–3：release.yml gui-build job 产出 universal .dmg 与 .AppImage/.deb，v0.5.0 实测上架——并当场抓到 publish 通配缺 GUI 二进制的缺陷，`check_gui_asset_globs` + fixture 6 已把 staged↔published 两份枚举机械比对；需求 4：Developer ID 证书本机 CSR 签发，CI 签名+公证+钉票在 runner 上实测 `accepted, source=Notarized Developer ID`；Windows .msi 记录为不做）
- FR-164 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/177-apply-audit-action-completeness.md` 与 `docs/qa/orchestrator/214-apply-audit-action-completeness.md` 承载（需求 1：`audited_mutation` 改为 `!dry_run` 恒真，dry-run 仍不审计。**step 0 发现缺口大于原述且改变了 FR 性质**：`begin` 是 `resolve_context` 的唯一调用者，而 `audited_mutation` 的首个析取项正是 `context.is_some()`——故 `enforced` 模式下无信封 apply 既不审计也不拒绝，开启强制得到的是虚假保证；DD-111 §21/§92 已将信封定为每一次 process-console mutation 的持久事实来源，故需求 1 属一致性修复而非设计修订，原文留给 step 0 的分支不适用。暴露面亦非原文推测的"裸 CLI 与脚本"：`orchestrator tool secret-rotate` 改写 SecretStore **值**却发 `audit: None`，现补 `operator_secret_rotate` 信封。原文"完全不产生审计行"表述不准且承重——`resource_versions`/`orchestrator_config_versions` 无条件写入，`author` 恒为字面量 `"daemon-apply"`，即不可归因而非不存在；验收标准因此改为点名 `control_action_audit`，否则可被一条无论如何都会写入的行满足。需求 2：`resource.<kind>.apply` 覆盖十个 kind，`source.template.apply`/`source.binding.apply` 保留既发布拼写（DD-111、QA 157 与既有审计行已记其名，为规整而重命名会使正确的记录失真）；动作名与 `target_type` 各由无 `_` 通配的穷尽 match 给出——第 13 个变体无法编译，由编译器而非手写清单担保集合派生自枚举。bundle 取单行聚合：`canonical_request` 按设计从不落库（`core/src/action_audit.rs:196-198` 只存其 SHA-256），故清单挂该字段既不可查询也不增加哈希熵——该"不可落库"已知限制入 DD，变通为按时间戳关联 `resource_versions`。需求 3：FR-157 词汇门禁无需扩展（其 TERMS 治理模式与理由码字面量，动作名不在该面内），EN+ZH CLI 指南补 apply 动作名一节。三个负夹具逐一实测：恢复旧析取项红 4 项（含 enforced 绕过——该 apply **成功**）、将 SecretStore 塌回通用名红 3 项且诊断点名该 kind、把轮换信封**注释掉**而非删除红 2 项而 `grep -c` 仍返回 2——文本存在性检查会把坏状态判为好。另记两项已知限制：`crates/integration-tests` 的 `TestOrchestratorServer::apply` 以重实现镜像该 RPC，绕过审计路径故无法观测本 FR 任何行为——值得审计该 harness 还重实现了哪些 RPC；`kind_as_str_covers_all_resource_kinds` 名为覆盖全部却只断言 12 个 kind 中的 3 个）
- FR-162 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/176-failure-visibility-contract.md` 与 `docs/qa/orchestrator/213-failure-visibility-contract.md` 承载（需求 1 三选一取**保留**：`ResolveTask` 携带 `preserve_kinds`/`reason`，任务完成清扫条件类项（理由分化为 `task_completed`）而保留证据类三 kind——`step_failed`、`low_confidence`、`task_spawn_failed`；`resume_executed` 保持全清扫，因 resume 通常正是从证据项上发起的操作员动作。同批次单事务场景由存储级测试与 QA 213 场景 1 直接建立，实现前基线以 `ORCHD`/`ORCH` 覆写实测目击红——六条 FAIL 逐一具名。需求 2：webhook 八个失败分支落 task-less external candidate——`source_auth_failed` 按 trigger 键、`source_route_missing` 按项目键且敌意名称只以摘要出现，30s 每键写节流封顶风暴成本，首次成功投递自愈，未知项目零分配，HTTP 响应逐字节不变；有意偏离原文"落 source event"——attention 行即持久投影，空 task_id 的 events 行对 JOIN tasks 的投影器结构性不可见。需求 3 取**推进 + 具名 gap**：flag 按项目而游标全局单行，不推进会让一个关闭的项目扣住所有项目的投影；m0038 gap 表与游标同事务折叠，重开 flush 为单条 `inbox_projection_gap` 项，fence 在行水位上。需求 4：《失败去了哪里》EN+ZH 落地，路由表/清扫规则/来源侧 kind 三个生成块由 `test-attention-routing-doc.sh`（ci-required，§4.4 shape 2 具名）从源码派生双向比对、ZH ≡ EN、空提取 fail closed、四个负夹具自证。需求 5 逐事件裁决入 DD-176，其中三处即修：臂拼写 `degenerate_cycle` 而发射者拼写 `degenerate_cycle_detected`——该臂从未触发过；`output_validation_failed` 路由为 `step_failed` 并补 `step_id`；`spawn_task` 失败与深度截断落 `task_spawn_failed` 持久事件并入保留集。**step 0 证伪了原文四处**：claim 1 对主执行路径是反的——驱动器步骤在 `validate_driver_events_stage` 早已按退出码硬失败，静默缺口只在 builtin/直连路径 + 默认 `on_failure: continue`；QA 211 的 wp05 目击实为 FR-156 apply 拒收，证词删除；`ResolveTask` 是审计化 UPDATE 非 DELETE；投影器在 scheduler crate 非 daemon。约 50 种未路由 kind 与 `trigger_error`/`trigger_skipped` 空 task_id 结构性不可投影问题记为 FR-167 候选）
- FR-158 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/172-governance-expansion-boundary.md`、`docs/design_doc/orchestrator/173-ratchet-masking-and-surface-closure.md` 与 `docs/qa/orchestrator/210-governance-system-introspection.md` 承载。**原文五条需求里有一条不必做、一条基于误读**，其余三条属实且要紧。（1）**需求 1 在立项之前就已闭环，且其证据本身是一次单一推导失误**：FR §1 报「governance job 有 36 处 `continue-on-error`，聚合清单只有 35 条」，但在它自己钉的 revision `9bcfaa96` 上实测是 **35 / 35、完全同步**；那个 36 来自 `grep -c 'continue-on-error'` 匹配到了 `ci.yml:167` 的一行**注释**——正是 `fr-governance` §6 规则 1 点名的 `grep -c` 失误，由写 FR 的审计者本人犯下。而该需求要求的「派生化」自 FR-137 起就已存在：`check_continue_on_error_aggregated` 从 `workflow_model.rb outcome-facts` 派生，覆盖**每个 workflow 的每个 job**（而非今天唯一有此类步骤的那一个），并断言三个方向——包括「步骤没有 id 因而结构上无法被聚合」这一 FR 的表述根本够不到的情形；fixture 22 就是 FR 验收标准 1 的逐字复现，22b 是它的正向对照。（2）**需求 4 的成本论据是误读**：FR 说「全 job 总和 2709s 已超 2700s 预算」，但 `ci-step-cost.json` 的 `budget.jobs` 明写该上限只覆盖 governance 与 ci-environment-parity 两个 job，实测该二者合计 **1793s / 2700s**（收尾时以 run 30792774882 刷新），尚有 907s 余量；FR 据一次并未发生的越界提出新设上限，而诚实的结论是**既有上限就是扩张预算**，它有效且远未逼近——DD-172 因此只是把它**命名**，没有发明新数字。（3）**需求 3 把两个不同的棘轮家族并成了一个**：DD-140 记的「四条行数正则」是 `coordination-governance.rb` 的四个坐标（按**行**计数），DD-148 记的「数散文中的驱动名」是 `persistence-dependency.rb`（按**词元出现次数**计数）；FR 把后者点名为「行数正则」的试点。两者都已修复。实测散文占比不是零头：`capturesOrJsonPath` 由 23 降到 **12**，原因是**拒绝** `behavior.captures` 的那个校验器在拒绝信息里写了这个字段名，于是删除该退役面的代码被算成使用该面的代码；每一条被移除的命中都逐条核对为注释或文档注释，真正的消费者（`workflow_steps.rs:78` 与 `:459` 的 `step.behavior.captures`）是代码、遮蔽后依然计入。**刻意不遮蔽的是 SQL 标尺**：`SQL_STATEMENT` 锚在开引号上，其主体就是被遮蔽抹掉的字符串字面量，统一两把标尺后全工作区读出 **0 条语句**——而且这个失败是安静的：门禁比对的是台账，若实施者接着跑 `--emit-baseline --write`，就会提交一份「零条 SQL」的台账和一个全绿的门禁，把条件 2 要压住的残留一并删掉。这是 §4.4 shape 10（修好欠命中的那一改，同时打开了过命中）。两个方向都做了变异实测：还原驱动遮蔽只挂 case 20（23 passed / 1 failed），统一两把标尺只挂 case 21（17 passed / 7 failed），两种签名不同，日志能说出坏的是哪一把。`core-boundary.rb` 数字纹丝不动（core 现在引用驱动 0 次、publicItems 遮蔽前后都是 611）却照样转换——在两边都读 0 的时候引入的分歧，正是没人会去找的那一种。（4）**需求 4 属实且规模比原文更大**：`check_surface_complete` 扫的是 `find scripts/qa`，122 个受追踪脚本里有 28 个在这个根之外，其中包含**所有 ci-required 门禁 source 的共享库**——`rust_source.rb`、`rust_lexer.rb`、`workflow_model.rb`、`gate_jq.sh`、`provider_isolation.sh` 等十二个。门禁被治理，门禁的引擎不被治理，而库里的缺陷是一次触达全部调用方的那种。根已改为 `scripts`，28 个**逐路径**分类，`scripts/spikes` 刻意不写成子树豁免（递归忽略会吸收尚不存在的文件且永远不在任何日志里留一行，§4.4 shape 8）。**一处 FR 没有的发现**：`WorkflowModel::SCRIPT_TOKEN` 只匹配 `.sh` 与 `.rb`——FR-147 精心把被执行集合改成派生，却把**扩展名**写成了清单——于是 `scripts/sync-docs.mjs` 由 `docs.yml` 每次推 main 都执行、又由 ci-required 的 `test-docs-publishing-integrity.sh` 再执行一遍，两处都对 check 14 不可见。它也不适配任何既有角色（`library` 声明「从不作为门禁本身被调用」，`release-tooling` 要求所有执行它的 workflow 都是 tag/dispatch 触发，而 docs.yml 跑在分支 push 上），故新增 `generator` 角色，其纪律是 `verifiedBy` 必须点名一个**存在且为 ci-required** 的门禁——少了这个字段，该角色就等于去掉了唯一条件的 release-tooling。（5）**watchdog 三条属实**：`docs/architecture.md` §7 称它「监控运行中的系统」并「恢复 .stable 快照**并重启服务**」，两样都不做——`--help` 体检的是磁盘上的可执行文件而非任何运行中的进程，所以一个卡死或在报错的守护进程每一轮轮询都能通过；恢复是 `cp` 加 `chmod +x`，什么都没重启。设计记录一直是对的（DD self-bootstrap/01 把「watchdog 管理的进程重启」列为非目标），所以是架构文档被改回它所描述的那份设计，脚本则以 `manual-runbook` 归入早已写好其 runbook 的 QA self-bootstrap/02，成为第 35 个人工门禁。（6）**需求 2 属实**：88 个门禁里 52 个每次推送都跑，另外 35 个由人执行而无人记录上次何时执行——FR-148 发现 `test-coordination-collapse.sh` 自 07-25 起坏掉、FR-149 发现 `test-wp05-integration.sh` 自 2026-03-26 起坏掉（四个月），两次都是靠读代码发现的，因为不存在可供发现的信号。新的 `scripts/lib/gate_runlog.sh` **与各门禁已有的 EXIT trap 组合而非替换**：35 个里有 30 个在那里清理守护进程或临时目录，再挂一个裸 `trap ... EXIT` 会静默丢掉前一个。六个拒绝在脏工作树上运行的门禁共用一个把台账按 pathspec 排除在外的谓词，否则它们会第一次通过、第二次因**记录器**的原因而失败——正是「因 fixture 自身原因而失败」那种病灶，只不过对象换成了门禁。台账起始全为 null，首份报告如实写「35 of 35 stale」，这就是验收标准 2 要的「显式 stale 列报」，而不是补造的历史。**新鲜度只报告、永不阻断**；会失败的是台账与清单对「哪些门禁是 manual-runbook」的集合不一致——一个不在台账里的门禁不会出现在任何一份报告里，而那读起来和「它很新鲜」一模一样（§4.4 shape 2）。**本 FR 施工期间由测试套件而非评审查出的三处缺陷，两处正是仓库自己警告的形状**：check 15 最初用 `grep -F` 匹配 arming，而被注释掉的调用同样含有那个字面串——§4.4 shape 1，出现在为执行 §4.4 而写的代码里，由 fixture 29（注释掉而非删除，因为删除才是作者想得到的那种）查出；behavioural 段跑一个故意退 7 的探针却没关 `set -e`，于是整轮在那里终止、汇总行从未打印——§4.4 shape 7；以及 check 15 重复断言了 check 1 已经拥有的「已声明但磁盘上不存在」，害得 fixture 2 不再隔离，已删除而非去重。**认证记录（`5faa8835`，前后同一 revision，worktree 前后皆干净）**：门禁集合由清单派生而非手写——`jq '.scripts[]|select(.enforcement=="ci-required").path'` 得 **53 条**（本 FR 前为 52），再用 `workflow_model.rb run-commands` 逐 workflow/job 取真实调用行反查，得 58 行含 `scripts/**` 的调用，其中引用 ci-required 路径的 **54 个调用**（含 `--fixture-test`、`--tool-fixtures` 等变体，以及 `certify-slack-managed-live.sh status` 这个裸跑会退 2 的子命令）；53 条里 44 条直接被调用、9 条经其声明的 `invokedBy` 包装器覆盖，**未覆盖集合为空**。每条以 `bash -c "$cmd" > log 2>&1` 直接取 `$?`，不经管道。**54/54 跑完，1 条红，非本 FR 回归**：`ci-liveness.rb`（十二个 job 的记录全部停在 `45fbf3c4`，而 ci.yml 此后已于 `ceccf4f5` 变更——两者都早于本 FR 的分支点，DD-171 已记录在案；它自己的 fixture 套件 9 passed / 0 failed 全绿，刷新需要一次真实 CI run）。54 份日志逐份确认非空且判决行存在——其中三份的判决行不是最后一行也不是 `passed, N failed` 形状（`certify-slack-managed-live.sh status` 是一张状态表、`ci-liveness.rb` 的 `CI liveness: FAIL` 在首行、`manual-gate-freshness.rb` 以「freshness report only」收尾），已逐一打开核对而非按末行形状推断。总时长 2699s（45.0 分钟），最慢三条为 `test-ci-environment-parity.sh` 952s、`test-agent-driver-execution-migration.sh` 413s、`test-filesystem-trigger.sh` 359s。`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets -- -D warnings` 均干净，`cargo test --workspace` **2859 passed / 0 failed / 2 ignored**（39 个 suite），三者退出码均直接取 `$?`。**四轮 sweep 因四个各不相同的真实原因作废，全部记入 DD-173 与 QA 210 而非静默修掉**：（1）修复「拒绝在脏树上运行」的六个门禁时，作用域取自 **manual-runbook** 集合，而该性质并不属于这个分类——另有六个 **ci-required** 门禁同样具备，其中两个还会调用人工门禁，于是被调用方记录了一次运行、写脏了受追踪的台账，`test-agent-driver-production-parity.sh` 因**记录器的**原因而非它自己的原因变红（§4.4 shape 9 第三条前提：足以覆盖眼前集合的作用域谓词，是关于那个集合的事实而不是关于这道检查的事实）；（2）上述修复用 perl 单行插入 source 行，perl 把 `$(` 当成 GID 变量内插，六个文件的路径全被写坏，而我用 `bash -n` 验证——**路径写坏的脚本语法完全正确**，这是 §4.4 shape 1 把编译器当成了 grep，六个门禁一次都没跑过就提交了；（3）本地 sweep 无 `CI` 变量，记录器按设计不该沉默、于是每轮都写脏台账——认证的前置条件应为 `CI=1`（也更贴近门禁真实的运行环境），此前无人写下；（4）`docs/feature_request/README.md` 的表格是由 `fr_registry.rb` 从 git 历史**生成**的（FR-155），删除 FR 文件后它即过期，而生成器给出的答案是一行状态为 `Closed` 的记录而非删掉该行——这一条由 `test-governance-ledger-tooling.sh` 查出，那道门禁离本 FR 改动的棘轮与清单都很远，没有任何线索会让人想到去看它，这正是「门禁集合必须派生而非凭印象挑选」的最好论据。另需记录一处**认证器自身**的缺陷：一次核对写成 `printf '... exit=%s ...' "$(basename $f)" "$?"`，命令替换先于 `$?` 展开并把它重置，读到的是 `basename` 的退出码——§4.6.4 的缺陷作用在认证者身上，已按该条改为立即存入变量后重新核对。
- FR-159 与 FR-150~158 的审计批次不同源：它来自 2026-08-02 对开发机运行态的一次直接观测（`ps` 实测 28 个存活 19 天的 fixture 会话进程与 6 个 `ppid=1` 仍在 LISTEN 的 orchestratord），而非文档/门禁面的静态审计。它是运行时资源泄漏而非结构债，因此优先级高于同期待办的 FR-157/158。立项时认为最值得注意的是 `cleanup_stale_sessions` 会删除被协调器标成 `failed` 的**活体**记录（先放弃回收，再销毁证据），但闭环期核实发现该函数零生产调用点，是潜伏缺陷而非在途泄漏；实际最要害的一层是**调度器的每一条 kill 路径都够不到会话**——tty 子进程从未进入 `runtime.child`，而唯一还能碰到它的 `kill_on_drop` 只发单 PID 而非进程组，于是组长被斩首、组内存活，这正是止血时观测到的形状。详见 DD-171
- FR-163 已闭环删除；其设计与验证信息现由 `docs/design_doc/orchestrator/178-runtime-layout-single-source.md` 与 `docs/qa/orchestrator/215-connectivity-path-single-source.md`、`docs/qa/orchestrator/216-daemon-readiness-and-connection-semantics.md` 承载。**规划核查推翻了 FR 原文的四项事实**：所谓「6 套 data_dir 派生机制」把 `lifecycle::socket_path`（接受 data_dir 为入参的纯 helper）与 115 处 `.data_dir` 线程化（消费方模式，正是需求 1 想要的终局形态）算了进去，真实为 4 处，故验收标准从「6 降至 1」改为「4 降至 2」——`discover_socket_path` 必须继续消费 `data_dir()` 不认识的 `ORCHESTRATOR_SOCKET`；「flat 与嵌套两种 db 布局同时活着」为**假**，7 处嵌套写法全在 `#[cfg(test)]` 内，生产只写 flat，所谓嵌套布局只是**调用方把自己的 data_dir 命名为 `data`** 时启发式的误判，**因此不需要任何 db 迁移**，这砍掉了需求 1 最大的一块；client 与规范版「回退语义不同」为假（两者都落到 cwd 相对 `.orchestratord`）；`fs_watcher` 的缺陷方向与原文相反——跳过规则只在 `ORCHESTRATORD_DATA_DIR` **已设置**时生效，而默认部署下该变量未设，故跳过是彻底的空操作，一个 root 落在 `$HOME` 的 filesystem trigger 会监视 daemon 自己的数据目录。另核实 `daemon status` **不走 gRPC**（读 pidfile + `kill -0`），原文列为未核验项的「鸡蛋问题」不存在；DD-175 全文无 `data_dir`，那条 known-limit 引用是错的；迁移数为 38 而非 37。**治理过程另发现并修复一处从未生效过的分支**：daemon 把客户端 bundle 写到 `~/.orchestrator/control-plane/config.yaml`，而传输发现第 4 步去找 `~/.orchestratord/...`，差一个字符——10 个以上门禁手工设 `ORCHESTRATOR_CONTROL_PLANE_CONFIG` 绕行即是其沉积物，而 DD-62 一直把路径写对，是代码偏离了自己的设计记录。需求 3 交付 `Health` RPC（聚合 `DbStatus`/`SecretKeyStatus`/`WorkerStatus`）、`orchestrator daemon status --wait-ready` 与 `gate_daemon_wait_ready`，收编 **24 处手抄轮询 / 23 个门禁 / 5 种超时预算（7.5–25s）**；helper 默认超时取五者中最宽的一个，因此这次迁移不可能引入原本不存在的 flake。需求 4 补齐 EN+ZH 三个连接语义主题（`ORCHESTRATOR_SOCKET` 治理前在 guide 中出现 **0** 次）并订正 quickstart 中「`init` 创建 SQLite 表结构」这句假陈述。
