---
name: design-system-guidance
description: Consult and apply the project design system when discussing UI design, visual language, design tokens, component variants, accessibility, theming, or styling conventions for the Web frontend. Use when a user asks about design system changes or implementing UI components/styles.
---

# Design System Guidance

When triggered:
1. Read `docs/design-system.md` first.
2. Treat tokens and component rules as constraints when proposing UI changes.
3. Ensure accessibility and performance fallbacks are preserved (especially for blur/backdrop effects).

In this repository, apply the constraints to the React/Vite frontend under `gui/` and its Tauri host under `crates/gui/`.
