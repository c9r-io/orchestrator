[package]
name = "{{project_name}}-core"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tower = "0.5"
http-body-util = "0.1"
serde_json = "1"
