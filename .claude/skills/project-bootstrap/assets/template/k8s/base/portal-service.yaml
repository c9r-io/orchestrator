apiVersion: v1
kind: Service
metadata:
  name: {{project_name}}-portal
  namespace: {{namespace}}
spec:
  selector:
    app: {{project_name}}-portal
  ports:
    - name: http
      port: 80
      targetPort: 80
