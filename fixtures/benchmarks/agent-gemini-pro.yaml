# Benchmark agent: Gemini CLI shell + gemini-3.1-pro-preview model
apiVersion: orchestrator.dev/v2
kind: Agent
metadata:
  name: bench-gemini-pro
  description: "Gemini CLI + gemini-3.1-pro-preview"
spec:
  capabilities:
    - plan
    - implement
    - qa_testing
    - qa_doc_gen
    - ticket_fix
    - align_tests
    - self_test
  command: gemini -p "{prompt}" -m gemini-3.1-pro-preview --yolo -o json
  promptDelivery: arg
  env:
    - fromRef: gemini-flash
  metadata:
    benchmark_shell: "gemini-cli"
    benchmark_model: "gemini-3.1-pro-preview"
