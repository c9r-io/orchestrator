# MiniMax: OpenCode uses its own provider auth; no extra secrets needed.
# If MiniMax requires an API key, set MINIMAX_API_KEY here.
apiVersion: orchestrator.dev/v2
kind: SecretStore
metadata:
  name: minimax
spec:
  data: {}
